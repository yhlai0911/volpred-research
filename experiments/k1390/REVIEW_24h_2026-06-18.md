# K1390 — 24h Post-Publish Review

- **Article**: `mile_5c8ef549` — K1390：VIX 體制拆分的 Conformal VaR — 唯一同時通過 Kupiec 與 Christoffersen 的方法
- **Published**: 2026-06-17 22:07 UTC
- **Review date**: 2026-06-18 (台灣時間)
- **Reviewer source**: `feature-dev:code-reviewer` subagent fallback（Codex CLI exec 0.139.0 stalled at prompt echo，無 review output；走 K1259 documented fallback path）
- **Audit scope**: `experiments/k1390/k1390.py` (398 lines) + `experiments/k1390/k1390_results.json` + 文章 vs results 數字對照

## 摘要 verdict

**CONDITIONAL_PASS** — 核心方法乾淨，4 項 WARN 需在 paper body 引用前補 caveat；published feed article 對 research audience 仍可保留。

## Findings

| # | 類別 | 嚴重度 | 結論 |
|---|------|--------|------|
| 1 | VIX lag 正確性（`vix[:-1]` line 125） | OK | 前一日 VIX 判 regime，無 lookahead |
| 2 | HS-252 rolling window 邊界（line 209） | OK | `returns[start:end]` exclusive `global_idx`，無 lookahead |
| 3 | IS/OOS CR 校準分離（line 160-193） | OK | CR high/low VaR 只用 IS bucket |
| 4 | Christoffersen indep guard 不對稱（line 65） | WARN | `pi1==0` 單獨不 early-return；現資料下 `eps=1e-300` 兜底不出問題，但 degenerate dataset 有 latent fragility |
| 5 | 文章數字 vs results.json | OK | CR α=0.05/0.01 的 Kupiec/CC p 與 VaR ratio 2.45×/2.77× 全對 |
| 6a | 12 p-values 無 multiple-comparison correction | WARN | Bonferroni 0.05/12≈0.004 下 CR 仍 pass，但 Harvey (2016) 要求書面標註 |
| 6b | CR α=0.01 indep sub-component p=0.046 | WARN | 邊界顯著；文章只報 CC joint p=0.123 過關，未揭露 ind 子分量 |
| 7 | 單一 OOS 期間 + 固定 cutoff/threshold | WARN | caveats 已提 sensitivity 未測；正文 REGIME_EFFECT 表述應降溫為「given calibration 下 consistent with regime effect」 |
| 8 | verdict logic `cr_cc_not_worse` 0.05 tolerance（line 337-339） | WARN | tolerance 無理論依據；現資料 CR 大勝 CU 不受影響，但是 ad hoc fragility |

## 對 article 的具體建議（補 caveat 即可，不必撤）

1. 「核心發現」段尾加一句：「12 個 p-value 未做 multiple-comparison correction；Bonferroni 校正後 CR 的最小 p 值仍遠高於 0.05/12，substantive 結論不變」
2. CR α=0.01 表格腳註：「CC 聯合 p=0.123 通過，但 Christoffersen 獨立性子分量 p=0.046 為邊界顯著」
3. 「結論」段：把 `REGIME_EFFECT` 字眼降溫；保留 caveats 中已列的 3 項 sensitivity 未測說明

## 對 source code 的後續修正建議（不阻塞 publish）

1. `christoffersen_independence` line 65 guard 改對稱：`pi0 == 0 or pi1 == 0` 任一即 early-return + 標 `np.nan`
2. `write_results` line 337-339 `cr_cc_not_worse` tolerance 0.05 改為文件化常數 + docstring 解釋理由，或改用 LR ratio 比較取代 absolute p-diff

## Codex CLI failure note

`codex exec --skip-git-repo-check --model gpt-5.4` 接收 prompt 後 exit 0 但 stdout 僅 echo prompt header + 任務描述，無 model output。Reasoning effort=xhigh 仍空轉。Diagnostic 順序（per `.claude/rules/experiments.md`）尚未跑（時間 budget 內走 documented fallback 較直接）；下次互動 session 可考慮 `codex --version` → `codex login status` → `cat ~/.codex/config.toml` → default-model smoke test。

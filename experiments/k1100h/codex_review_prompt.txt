Code review for K1100h Phase 1 v2 (TAIFEX intraday-feature daily PRG).

Context: v1 was Codex primary-path FAIL (2 MAJOR + 2 MINOR). Main thread fixed all 4 issues and re-ran. Verdict went from BORDERLINE (DM-t=2.85) to BORDERLINE (DM-t=2.21) — fix direction is conservative shrinkage as expected.

Your job: verify all 4 v1 issues are correctly resolved AND no new issues introduced. Verdict CONDITIONAL PASS+ unless you find a substantive bug.

## Files to review (absolute paths)
- experiments/k1100h/k1100h_load_taifex.py — bar aggregation, encoding fallback
- experiments/k1100h/k1100h.py — main PRG estimation, OOS, DM
- experiments/k1100h/k1100h_v2_codex_review.md — main thread's self-review (read this first to see the proposed fixes; then verify the code matches)
- experiments/k1100h/k1100h_results.json — v2 results (verdict BORDERLINE, DM-t M3=2.21)

## v1 Issues + v2 Fixes to verify

### MAJOR 1: bar aggregation 13:45 endpoint mis-binning
- v1: 423/1138 days had 61 bars (13:45:00 closing tick floored to its own bar)
- v2 fix: `k1100h_load_taifex.py` `build_5min_bars` — after `dt.floor("5min")`, mask any day-session bar_start >= 13:45:00 of file_date and reset to 13:40:00
- Verify: (a) logic correctly identifies endpoint without affecting opening tick (08:45:00 → bar_start=08:45 ✓); (b) re-built cache shows 0 days with >60 bars (1082/1138 = 60 bars; 56 days <60 bars are real TAIFEX half-day early-closes)
- Question: is this surgical mask logically equivalent to right-closed binning for [08:45, 13:45]? Any edge case missed (e.g. tick at 13:45:01 from a system glitch)?

### MAJOR 2: roll/settlement handling on TX1.csv
- v1: only filtered K1100g `is_roll==True` (61 days, day AFTER settlement), NOT settlement day itself. TX1 on 3rd Wednesday = expiring contract → contaminated intraday microstructure.
- v2 fix: `k1100h.py` post-merge — drop both `is_roll==True` AND `is_settlement==True` (where `is_settlement` already exists in K1100g cache via `_is_third_wednesday`)
- Sample-size impact: 1138 → 1082 (after roll drop, -56) → 1026 (after settle drop, -56) → OK
- Question: is dropping settlement days the correct fix vs. re-implementing TX-most-volume-picker (which is what K1100g does)? Asymmetry vs K1100g_d5 baseline is the user-flagged concern.

### MINOR 1: Big5 header validation
- v1: hardcoded `encoding="big5"` no fallback, no schema check
- v2 fix: `load_one_file` — try big5 / cp950 / utf-8 chain; assert df.shape[1] == 10 cols
- Verify: re-run logged 1223 files parsed without error.

### MINOR 2: HAC lag in DM test
- v1: `lag = floor(n^(1/3))` (could be 0 for tiny n)
- v2 fix: `dm_test_hln` — `lag = max(1, floor(4*(n/100)^(2/9)))` (Newey-West 1994 automatic bandwidth)
- Question: is NW1994 the right standard for HLN-corrected DM? Any reason to prefer Andrews-Monahan?

## Lookahead discipline (CRITICAL — verify in v2 code, NOT just trust v1 audit)
Walk through `_prg_variance_recursion_multi(params, r, dow_dum, exog_mat=mom_z)` step-by-step:
1. At loop iteration t=k, what is the most recent index of `r` accessed? (expected: k-1)
2. What is the most recent index of `exog_mat` accessed? (expected: k-1)
3. Confirm exog at index k-1 = features computed from day k-1's intraday tick stream (i.e. all realized before day k 08:45 open)
4. Standardization: `standardize(rv5, train_idx)` computes μ,σ from `rv5[train_idx]` only and applies to full `rv5` array. Confirm this is leakage-clean (test-window data does not enter μ,σ).
5. Seed: confirm `np.random.seed(42)` + `RNG = default_rng(42)` + per-fit `local_rng = default_rng(SEED)` covers all stochastic init.

## Output format

Provide:
- **VERDICT**: one of {PASS, CONDITIONAL PASS, FAIL}
- **MAJOR ISSUES**: 0 or list with file:line + description + severity
- **MINOR ISSUES**: 0 or list
- **POSITIVE FINDINGS**: 1-3 bullets on what was done correctly
- **RECOMMENDATIONS**: optional follow-up suggestions

Keep response under 1500 words. Direct verdict; do not hedge unless genuinely uncertain.

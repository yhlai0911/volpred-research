# K1628 Codex Code Review

**Reviewer**: Codex CLI primary path  
**Date**: 2026-07-05（台灣時間）  
**Verdict**: **PASS**

## Scope

Review target: `experiments/k1628/k1628.py`  
Task: 投資迷思驗證「黃金真的是股災避風港嗎？」危機期間條件相關性。

This review checks research honesty, data alignment, return calculation, statistical formulas, bootstrap reproducibility, and whether the same-day design is correctly framed as descriptive rather than tradable.

## Findings

| 項目 | 結果 |
|---|---|
| Data source / sample | PASS — SPY/GLD/^VIX all read from `data/cache/price_cache.db`; SPY/GLD common sample 2016-01-05 to 2026-07-02, n=2638 |
| Return calculation | PASS — SPY and GLD each use their own `adj_close` fallback-to-close series and `pct_change()`; first-row NaN is dropped only when building the common panel |
| Lookahead / alignment | PASS — same-day SPY/GLD returns are intentional for safe-haven co-movement, and the script explicitly states this is not a forecast or trading signal; VIX same-day close is only descriptive |
| Conditional universe | PASS — all conditional metrics are computed from the same common SPY/GLD event universe; thresholds are defined once from the same panel |
| Wilson / Fisher-z CI | PASS — Wilson positive-rate CI and Fisher-z correlation CI formulas are correct and guard small-n cases |
| HAC tests | PASS — GLD conditional mean tests and safe-haven regression use statsmodels HAC with `maxlags=5`; R² reported from non-robust OLS fit, which is standard |
| Bootstrap | PASS — circular block bootstrap uses block=5, B=2000, seed=42; reproduced exactly for `SPY<-2%` |
| Baur-style regression | PASS — non-overlapping crash bins avoid nested-dummy ambiguity; conditional slopes are reported separately and interpreted conservatively |
| Edge cases | PASS — empty/small samples return NaN rather than divide-by-zero; current sample sizes are adequate except `SPY<-3%` n=33, which README flags as a caveat |
| Output artifacts | PASS — `k1628_results.json` and both PNG figures are generated by the script; figure titles match the measured result and do not overclaim |

## Repro Checks

Commands run:

```bash
uv run python -m py_compile experiments/k1628/k1628.py
uv run python experiments/k1628/k1628.py
```

Additional read-only checks:

- `panel_rows=2638` matches `k1628_results.json`.
- SPY/GLD first return rows are NaN before panel construction.
- Manual second-row pct_change matches code output for SPY and GLD.
- `SPY<-2%` condition recomputation matches JSON:
  - n=92
  - GLD positive rate=0.4565217391
  - SPY-GLD corr=0.2482102132
- Bootstrap `SPY<-2%` with seed=42 reproduces JSON exactly.
- SQLite check: SPY and GLD each have 2639 rows from 2016-01-04 onward, `adj_close` null count=0, date count=2639.

## Verdict Rationale

The core conclusion is supported by the code and is appropriately limited:

> GLD has low average correlation with SPY, but in 2016-2026 stock stress days it does not reliably become negatively correlated or positive-returning. Gold is a weak, regime-dependent diversifier, not an automatic crash hedge.

No blocking issue found. Results are suitable for knowledge entry and draft article use, with the README caveats preserved.

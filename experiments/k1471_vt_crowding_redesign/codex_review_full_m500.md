# K1471 Codex adversarial review (codex-cli 0.137.0, 2026-06-11)

Prompt: /tmp/k1471_codex_review_prompt.txt (archived below findings)

1. `HIGH` `_sup_wald` + label permutation 在數學上只能檢定「adoption 標籤可交換／曲線無序結構」，不能檢定「存在離散單一 break」；目前文件與輸出有混淆。[k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:476) 的統計量其實是把 `groups[:k+1]` 與 `groups[k+1:]` 全池化後做 Welch 型均值差，然後在 [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:543) 把 pooled Sharpe 直接 permutation；這個 null 是「flat / exchangeable」，不是「smooth monotone but no break」。但檔頭與 detector 文案多次寫成「single-break structural test / breakpoint detector」[k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:14), [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:499), [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:881)。所以 `VT_baseline p=0.001` 合法的解讀是「拒絕 flatness」，不合法的解讀是「證明有離散 tipping point」。

2. `HIGH` `threshold = first post-break level only if significant AND degradation` 這個抽取規則本身可作為描述性 rule，但如果摘要表不一起報 `degradation_direction`，`p=0.001, threshold=null` 會嚴重誤導。規則寫在 [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:558)；摘要表卻只輸出 `status / threshold / p-value / interval / baseline Sh`，完全沒放 direction 或 pre/post mean [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:919)。`RR_VT` 在 5 個 cell 都是顯著但 `degradation_direction=false`；例如 cell1 是 `pre=0.477, post=0.541, p=0.001, threshold=null`。如果 summary table 只看 `null`，讀者會以為「沒有顯著變化」，但真相是「有顯著非平坦性，只是方向是改善」。

3. `HIGH` `VT_baseline threshold=100%` 明顯帶有 grid artifact，不能被說成精確定位的 tipping point。cell1 各 split 的 sup-Wald 我重算後依序是 `428, 879, 1307, 1859, 2320, 5708`，最大值出現在 `70% -> 100%`；對應均值差從前一個 split 的 `0.478` 放大到 `0.609`。這不是因為統計量「機械偏好寬 gap」本身直接吃進 30pp，而是因為演算法完全不使用 adoption 距離，只在既有網格上挑最大 prefix-vs-suffix 均值差 [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:483)。在 70% 與 100% 中間沒有 80/90% 點時，`threshold=100%` 只代表「最後一格最差、最後一個 split 最強」，不能代表「真正斷點就在 100%」。對你要支持的敘述，最多只能說「在目前網格上最大惡化出現在 70%-100% 區間」。

4. `HIGH` RR_MR 的 turnover-matching 設計在病態 treatment 上失效，`RR_MR 30%` 不應被解讀成 crowding 證據。stage 2 直接把 stage 1 的 `_turnover` 餵進 RR control [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:830)。但 MR 30% 的 matched input 本身已病態：cell1 `dw_mean=2.84, freq=0.974`，遠大於權重上限；RR agent 隨後又在 [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:277) 被 `clip`。實際跑出來的 RR_MR turnover 已不再 matched：cell1 `matched_input dw_mean=2.84/freq=0.974`，但 `RR_MR actual dw_mean=2.15/freq=0.625`。這說明 control 繼承的是 treatment 的 collapse footprint，再被 clipping 扭曲，不是可解釋的「matched random rebalance」。這裡需要 input gate，例如拒收超 cap 的 `dw_mean`、極端 `freq`、或 treatment 出現大量 `price_clamp` 時直接標記 matched control 不可用。

5. `HIGH` `pr / pv if pv > 0 else 0.0` 對 collapsed-market sim 是 detector-validity bug，不只是 cosmetic fallback。[k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:441) 會把 `pv==0` 的路徑靜默改成 Sharpe `0.0`。`jq` 檢查顯示 exact-zero Sharpe 只出現在 `MR` 的三個 `30%` 組：`cell1/cell4/cell5` 各 `500/500` 全為 0；這些組同時有 `total_price_clamps=1000`、`final_price mean≈2.27e-14`。雖然 MR detector 因 baseline gate 沒被正式解讀，但這些假零值仍進入了 stage-1 summary、robustness、以及 RR_MR matching 輸入。更合理的做法是把這類 path 標成 `None/NaN` 或單獨 collapse flag，禁止當成有效 Sharpe 參與後續 calibration。

6. `MED` 目前沒有多重檢定控制，而且總共做了 35 個 detector run；因此所有「strategy-specific」或跨 cell 的 inferential 說法都要保守。程式在每個 `cell × treatment` 都跑 detector [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:853)，但沒有 family-wise 或 FDR 修正。`p=0.001` 的 VT 結果即使 Bonferroni 35 仍勉強存活，但 `p=0.003` 這種邊緣值就不一定。這不推翻「VT 非平坦、RR_VT 改善」的描述性結論，但會削弱任何把單格 p-value 當成強 confirmatory 證據的寫法。

7. `LOW` seed 與 RNG stream 沒看到 lookahead 或明顯 collision bug，但有一個應明講的設計事實：VT/TF/MR/Noise 共用同一個 base seed offset 0，而 RR 額外用 `RR_RNG_OFFSET` 開獨立決策流。[k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:160), [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:300), [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:315), [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:701)。這代表 market/noise shocks across treatments 是 common-random-numbers 設計，不是獨立 treatment worlds。這本身可接受，甚至有助降噪，但論文應明說，不然讀者會誤以為 treatment 間 p-values 是獨立實驗。

8. `LOW` 單元測試不足以保護本次最關鍵的方法論風險。[test_detector_unit.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/test_detector_unit.py:16) 只測了「明顯離散 break」、「flat」、「saturated」、「determinism」與 RR turnover sanity；沒有測「平滑單調下降但無離散 break」會被顯著拒絕、沒有測 `p<0.05 但 degradation=false` 的 `threshold=null` 案例、也沒有測 `pv==0` collapse 路徑。這讓目前輸出很容易被過度詮釋而測試不會報警。

9. `LOW` bootstrap breakpoint interval 的呈現也偏誤導。函式會在所有 bootstrap replicate 都記錄 argmax split，即使原檢定不顯著；而 numeric interval 還會把 `no_degradation/degenerate` 排除，只對剩下的百分比標籤取 80% 區間 [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:565)。因此像 `NoiseControl` 雖然 `p=0.953, threshold=null`，仍可拿到 `[40%,100%]` 的 interval。這種 interval 不應在摘要表中與正式 threshold 同列，否則會給讀者假的定位感。

VERDICT: CONDITIONAL PASS

可支持的說法是：`VT Sharpe` 隨 adoption 單調惡化、`RR_VT` 沒有 degradation 且方向反而改善、因此「70% tipping point」不成立。不可直接支持的說法是：存在被 sup-Wald 證明的離散 break，或可精確說「最大下降發生在 100% threshold」。更精確的版本應改成：「在目前 adoption grid 上，VT 的最大額外惡化出現在 70%-100% 區間；sup-Wald 只拒絕 flatness，不足以證成離散 tipping point。」另外，`RR_MR` 結果應視為 matched-control 失效，不應拿來當 crowding 證據。
tokens used
59,321
1. `HIGH` `_sup_wald` + label permutation 在數學上只能檢定「adoption 標籤可交換／曲線無序結構」，不能檢定「存在離散單一 break」；目前文件與輸出有混淆。[k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:476) 的統計量其實是把 `groups[:k+1]` 與 `groups[k+1:]` 全池化後做 Welch 型均值差，然後在 [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:543) 把 pooled Sharpe 直接 permutation；這個 null 是「flat / exchangeable」，不是「smooth monotone but no break」。但檔頭與 detector 文案多次寫成「single-break structural test / breakpoint detector」[k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:14), [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:499), [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:881)。所以 `VT_baseline p=0.001` 合法的解讀是「拒絕 flatness」，不合法的解讀是「證明有離散 tipping point」。

2. `HIGH` `threshold = first post-break level only if significant AND degradation` 這個抽取規則本身可作為描述性 rule，但如果摘要表不一起報 `degradation_direction`，`p=0.001, threshold=null` 會嚴重誤導。規則寫在 [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:558)；摘要表卻只輸出 `status / threshold / p-value / interval / baseline Sh`，完全沒放 direction 或 pre/post mean [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:919)。`RR_VT` 在 5 個 cell 都是顯著但 `degradation_direction=false`；例如 cell1 是 `pre=0.477, post=0.541, p=0.001, threshold=null`。如果 summary table 只看 `null`，讀者會以為「沒有顯著變化」，但真相是「有顯著非平坦性，只是方向是改善」。

3. `HIGH` `VT_baseline threshold=100%` 明顯帶有 grid artifact，不能被說成精確定位的 tipping point。cell1 各 split 的 sup-Wald 我重算後依序是 `428, 879, 1307, 1859, 2320, 5708`，最大值出現在 `70% -> 100%`；對應均值差從前一個 split 的 `0.478` 放大到 `0.609`。這不是因為統計量「機械偏好寬 gap」本身直接吃進 30pp，而是因為演算法完全不使用 adoption 距離，只在既有網格上挑最大 prefix-vs-suffix 均值差 [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:483)。在 70% 與 100% 中間沒有 80/90% 點時，`threshold=100%` 只代表「最後一格最差、最後一個 split 最強」，不能代表「真正斷點就在 100%」。對你要支持的敘述，最多只能說「在目前網格上最大惡化出現在 70%-100% 區間」。

4. `HIGH` RR_MR 的 turnover-matching 設計在病態 treatment 上失效，`RR_MR 30%` 不應被解讀成 crowding 證據。stage 2 直接把 stage 1 的 `_turnover` 餵進 RR control [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:830)。但 MR 30% 的 matched input 本身已病態：cell1 `dw_mean=2.84, freq=0.974`，遠大於權重上限；RR agent 隨後又在 [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:277) 被 `clip`。實際跑出來的 RR_MR turnover 已不再 matched：cell1 `matched_input dw_mean=2.84/freq=0.974`，但 `RR_MR actual dw_mean=2.15/freq=0.625`。這說明 control 繼承的是 treatment 的 collapse footprint，再被 clipping 扭曲，不是可解釋的「matched random rebalance」。這裡需要 input gate，例如拒收超 cap 的 `dw_mean`、極端 `freq`、或 treatment 出現大量 `price_clamp` 時直接標記 matched control 不可用。

5. `HIGH` `pr / pv if pv > 0 else 0.0` 對 collapsed-market sim 是 detector-validity bug，不只是 cosmetic fallback。[k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:441) 會把 `pv==0` 的路徑靜默改成 Sharpe `0.0`。`jq` 檢查顯示 exact-zero Sharpe 只出現在 `MR` 的三個 `30%` 組：`cell1/cell4/cell5` 各 `500/500` 全為 0；這些組同時有 `total_price_clamps=1000`、`final_price mean≈2.27e-14`。雖然 MR detector 因 baseline gate 沒被正式解讀，但這些假零值仍進入了 stage-1 summary、robustness、以及 RR_MR matching 輸入。更合理的做法是把這類 path 標成 `None/NaN` 或單獨 collapse flag，禁止當成有效 Sharpe 參與後續 calibration。

6. `MED` 目前沒有多重檢定控制，而且總共做了 35 個 detector run；因此所有「strategy-specific」或跨 cell 的 inferential 說法都要保守。程式在每個 `cell × treatment` 都跑 detector [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:853)，但沒有 family-wise 或 FDR 修正。`p=0.001` 的 VT 結果即使 Bonferroni 35 仍勉強存活，但 `p=0.003` 這種邊緣值就不一定。這不推翻「VT 非平坦、RR_VT 改善」的描述性結論，但會削弱任何把單格 p-value 當成強 confirmatory 證據的寫法。

7. `LOW` seed 與 RNG stream 沒看到 lookahead 或明顯 collision bug，但有一個應明講的設計事實：VT/TF/MR/Noise 共用同一個 base seed offset 0，而 RR 額外用 `RR_RNG_OFFSET` 開獨立決策流。[k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:160), [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:300), [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:315), [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:701)。這代表 market/noise shocks across treatments 是 common-random-numbers 設計，不是獨立 treatment worlds。這本身可接受，甚至有助降噪，但論文應明說，不然讀者會誤以為 treatment 間 p-values 是獨立實驗。

8. `LOW` 單元測試不足以保護本次最關鍵的方法論風險。[test_detector_unit.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/test_detector_unit.py:16) 只測了「明顯離散 break」、「flat」、「saturated」、「determinism」與 RR turnover sanity；沒有測「平滑單調下降但無離散 break」會被顯著拒絕、沒有測 `p<0.05 但 degradation=false` 的 `threshold=null` 案例、也沒有測 `pv==0` collapse 路徑。這讓目前輸出很容易被過度詮釋而測試不會報警。

9. `LOW` bootstrap breakpoint interval 的呈現也偏誤導。函式會在所有 bootstrap replicate 都記錄 argmax split，即使原檢定不顯著；而 numeric interval 還會把 `no_degradation/degenerate` 排除，只對剩下的百分比標籤取 80% 區間 [k1471_vt_crowding_redesign.py](/Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/k1471_vt_crowding_redesign.py:565)。因此像 `NoiseControl` 雖然 `p=0.953, threshold=null`，仍可拿到 `[40%,100%]` 的 interval。這種 interval 不應在摘要表中與正式 threshold 同列，否則會給讀者假的定位感。

VERDICT: CONDITIONAL PASS

可支持的說法是：`VT Sharpe` 隨 adoption 單調惡化、`RR_VT` 沒有 degradation 且方向反而改善、因此「70% tipping point」不成立。不可直接支持的說法是：存在被 sup-Wald 證明的離散 break，或可精確說「最大下降發生在 100% threshold」。更精確的版本應改成：「在目前 adoption grid 上，VT 的最大額外惡化出現在 70%-100% 區間；sup-Wald 只拒絕 flatness，不足以證成離散 tipping point。」另外，`RR_MR` 結果應視為 matched-control 失效，不應拿來當 crowding 證據。

---
## Review prompt
```
You are an adversarial methodology reviewer for a simulation experiment. Work dir: /Users/yhlai0911/Desktop/volpred-research/experiments/k1471_vt_crowding_redesign/

Files to review:
- k1471_vt_crowding_redesign.py (main script; focus on detect_threshold_exogenous, _sup_wald, permutation null, bootstrap CI functions, RandomRebalanceAgent, turnover recording in run_single_simulation)
- k1471_full_results.json (M=500 results; use jq, do NOT read whole file)
- test_detector_unit.py

Context: This is a redesign of a VT-crowding agent-based model. The original paper claimed a "70% adoption tipping point" for volatility-targeting (VT) Sharpe degradation. The new exogenous sup-Wald detector with M=500 finds:
1. VT_baseline: significant break (p=0.001) with threshold=100% in all 5 cells; but the Sharpe-vs-adoption curve declines monotonically (cell1: 0.51, 0.48, 0.40, 0.34, 0.20, 0.09, -0.27 at 10/30/40/50/60/70/100%).
2. RR_VT (turnover-matched random control for VT): significant sup-Wald (p=0.001-0.003) but degradation_direction=false (Sharpe IMPROVES 0.447 to 0.541), so threshold=null by the extraction rule (threshold only if significant AND post_mean < pre_mean).
3. RR_MR shows a wild Sharpe spike of -2.31 at adoption=30% (vs -0.007 at 10%, -0.33 at 40%) in cells 1/4/5 because the matched turnover params inherit MR's pathological footprint at 30% (MR collapses the market there: final_price ~ 2e-14, all 500 sims Sharpe = exactly 0 via the pv>0-else-0.0 fallback).
4. RR_TF declines gradually (-0.04 to -0.31).

Adversarial review tasks (be specific, cite line numbers):
A. Is the _sup_wald + label-permutation p-value implementation mathematically sound? Note: a smooth monotone trend is NOT the permutation null (null = exchangeability/flat), so p=0.001 rejects flatness, not "discrete break exists". Does the code/output documentation conflate these? Any other multiple-testing or pooling issues?
B. Is the threshold extraction rule (threshold = first post-break level only if significant AND degradation) sound, and is reporting "p=0.001, threshold=null" for RR_VT misleading without the direction field in the summary table?
C. The widest grid gap is 70%->100% (30pp vs 10pp steps in cell1). For a smooth declining curve, does the sup-Wald argmax mechanically favor the widest gap? Quantify intuition. Does that make "threshold=100%" partly a grid artifact?
D. Turnover-matching design: stage-1 measures treatment turnover, stage-2 feeds it to RR controls. When the treatment regime is pathological (MR market collapse), the matched control inherits a meaningless footprint. Is the RR_MR "break at 30%" interpretable as crowding evidence? Should there be a gate on matched-control inputs?
E. The Sharpe fallback `pr/pv if pv > 0 else 0.0` (line ~441) silently converts collapsed-market sims into Sharpe=0, which then enters detector groups (MR 30% has 500 sims of exactly 0.0). Is this a bug for detector validity? (MR is gated by APPLICABILITY_FLOOR=-0.5 via baseline group, but is the 0.0-contamination still a problem anywhere else? Check whether any non-gated treatment group contains such degenerate zeros: e.g. jq query on sharpe_sims arrays for exact-zero runs.)
F. Any lookahead, seed-collision (TREATMENT_SEED_OFFSET vs base formula), or RNG stream-sharing issues (e.g. RR agent rng vs market rng independence)?
G. Verdict: PASS / CONDITIONAL PASS / FAIL for using these M=500 results to support the claim: "VT Sharpe erosion is monotonic in adoption and strategy-specific (matched random-rebalance control shows no degradation); no discrete 70% tipping point; the largest decline occurs between 70% and 100% adoption."

Output: numbered findings with severity (HIGH/MED/LOW), then final verdict line "VERDICT: ...".
```

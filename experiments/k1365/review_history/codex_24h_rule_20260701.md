# K1365 / mile_c16d94b7 — Codex 24h-rule Source-Level Review

**Reviewer**: Codex (gpt-5.x)
**Date**: 2026-07-01
**Article**: mile_c16d94b7 (published 2026-06-30T16:00Z)
**Verdict**: FAIL_MAJOR

## Findings

### CRITICAL / MAJOR (research honesty / correctness)

- **MAJOR — `forward5_rv` regression uses a future-contaminated lagged target control.** The signal side is lagged correctly, but `forward5_rv` is built as `primary_ret.rolling(5).std().shift(-4)` in `experiments/K1365/K1365.py:283`, so row `t` contains returns from `t` through `t+4`. The regression then sets `target_lag1 = gp.targets[target_name].shift(1)` in `experiments/K1365/K1365.py:329`. For `target_name == "forward5_rv"`, row `t` therefore controls for `forward5_rv_{t-1}`, which includes returns from `t-1` through `t+3`; at the `t-1` forecast origin, returns `t..t+3` are not observable. This contaminates every `forward5_rv` regression, including the public article's most prominent EM claim.

- **MAJOR — the article quotes the contaminated EM `-5.3` statistic as a strong signal.** Article line 40 says EEM/IEMG "檢定值衝到負 5.3"; that matches `K1365_regression_table.csv:77` (`leader_share_z_l1`, `forward5_rv`, HAC t = -5.34036), but that row is affected by the `forward5_rv` lag-control leakage above. A quick audit recomputing the same EM forward5 regression with the previous completed 5-day block as the control (`forward5_rv.shift(5)`, not a full replacement for a formal rerun) reduces the leader-share HAC t-stat from about `-5.34` to about `-2.65`. The reverse EM pattern still has non-forward5 support (`next_range_vol`, dispersion, tracking range), but the exact `-5.3` public claim is not source-clean.

### MINOR / SUGGESTION

- `K1365_results.json` has `"lookahead_protected": true` even though the forward5 control is not lookahead-clean. After the rerun, make this field target-specific or only set it true after a direct information-set check.
- The feed metadata verdict says "EM reverse signal (5 sig results all negative, all EM)". That is defensible only if it counts all negative significant rows including HHI; the primary leader-share count is 4 negative Harvey+BH rows, while the README table displays 3 non-duplicative rows. Use clearer wording if this metadata is surfaced publicly.
- Harvey `+3` is acceptable as a conservative discovery bar, but article wording should keep it as a project discovery threshold, not a universal significance rule.

## 逐項 check 結果

1. Lookahead — **FAIL (forward5 control), PASS (signal lag)**. Rolling z-scores are computed in `experiments/K1365/K1365.py:162-165` and all liquidity signals are shifted one day in `experiments/K1365/K1365.py:262-270`, so the concentration signal at row `t` uses information available through `t-1`. However, for `forward5_rv`, `target_lag1` in `experiments/K1365/K1365.py:329` is not known at the forecast origin because the forward target was constructed in `experiments/K1365/K1365.py:283` with `shift(-4)`.

2. HAC / Newey-West — **PASS minimum standard**. `HAC_LAGS = 5` in `experiments/K1365/K1365.py:59` and `cov_type="HAC", cov_kwds={"maxlags": HAC_LAGS}` is used in `experiments/K1365/K1365.py:367`. For an overlapping 5-day target, `maxlags >= horizon` is a reasonable minimum. A robustness rerun with larger lags or non-overlapping weekly targets would still be cleaner for publication.

3. Article claim vs results.json — **PARTIAL PASS / ONE MAJOR MISMATCH IN CLEANNESS**.
   - `SPY 91.9% / QQQ 98.5% / IWM 99.0% / EEM 80.5%`: **matches** `K1365_results.json` group means and article lines 13 and 19-22.
   - "十六個主要檢定裡，沒有任何一個朝「越集中、越震盪」的方向成立，零個": **matches** `primary_summary.n_positive_harvey_t_ge_3 = 0` and all 16 primary leader-share rows have `positive_harvey_t_ge_3 = false`.
   - "新興市場 EEM/IEMG 檢定值衝到負 5.3": **numerically matches but is not clean evidence**. It matches `K1365_regression_table.csv:77`, but that row is a contaminated `forward5_rv` regression. Direction interpretation is correct: a negative coefficient on leader share means lower EEM leader share / more dispersion is associated with higher subsequent volatility.
   - Harvey `+3` threshold: **PASS**. `HARVEY_T = 3.0` is defined in `experiments/K1365/K1365.py:62`, and `positive_harvey_t_ge_3` is explicitly computed in `experiments/K1365/K1365.py:409-410`. README lines 60 and 74-80 also frame it as a discovery bar.

4. Overclaim — **FAIL for the EM `-5.3` forward5 claim; PASS for the main NULL_PROXY claim**. The article does not overclaim positive leader-concentration predictability, and the `NULL_PROXY` verdict is consistent with 0/16 positive primary Harvey hits. The overclaim is quoting the strongest reverse forward5 statistic without a clean information set.

5. Reader-honest hedging — **PASS overall**. Article lines 44-46 clearly state the free-data proxy limitation and do not claim to prove ETF liquidity segmentation has no volatility effect. Article line 42 uses tentative mechanism language ("更可能反映"), not a discovered mechanism claim. After rerun, the EM paragraph should explicitly call the reverse result a proxy/pilot finding.

## Recommended fixes（若非 PASS）

1. Fix `experiments/K1365/K1365.py:329` by making the target control information-set aware. Minimal repair:

```python
def known_target_control(gp: GroupPanel, target_name: str) -> pd.Series:
    if target_name == "forward5_rv":
        # Previous completed 5-trading-day block, known at the t-1 forecast origin.
        return gp.targets[target_name].shift(5)
    return gp.targets[target_name].shift(1)
```

Then use `"target_lag1": known_target_control(gp, target_name)` in the raw regression frame. Prefer renaming the field/model label to `target_known_lag` so the timing is explicit.

2. Rerun:

```bash
uv run python experiments/K1365/K1365.py
```

Then update `experiments/K1365/K1365_results.json`, `experiments/K1365/K1365_regression_table.csv`, `experiments/K1365/README.md`, and figures derived from the forward5 row.

3. Patch the published article before treating it as reviewed. At minimum, remove or replace the `-5.3` claim in `/tmp/mile_c16d94b7_article.md` / feed content and add a correction note that the forward5 statistic was rerun with a known-at-origin control. If the clean rerun no longer clears the `t >= 3` bar, rewrite the EM section around the non-forward5 reverse evidence (`next_range_vol` t = -3.22; same-index dispersion/tracking t = -3.02) and label it as pilot/proxy evidence.

4. Add a small timing assertion or audit table to K1365 output: for each regressor, record `information_end <= forecast_origin`. This would have caught the `forward5_rv.shift(1)` issue.

## Summary

The main public null — no positive evidence that leader ETF volume-share concentration predicts higher volatility — is supported by `0/16` positive primary Harvey hits. The article still fails the 24h source-level rule because its strongest EM reverse statistic (`forward5_rv` HAC t about `-5.3`) comes from a forward-target regression with lookahead-contaminated lag control; rerun and article correction are blockers before PASS.

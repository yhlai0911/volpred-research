# K1095 v3 remediation brief

Work only in this registered worktree:

`/Users/yhlai0911/volpred-research/.claude/worktrees/dispatch-slot-1-6ec34837-k1095v3`

Source task: `k1095_v3_remediation`. The worktree is pinned to the adjudicated
K1095-v2 commit `7983a1642cf72f8e09d22764a5e3b84fc31f2c51`.

## Objective

Create a new, self-contained `experiments/k1095_v3/` experiment that remedies
all four defects below. Preserve `experiments/k1095_v2/` byte-for-byte as the
failed historical artifact. Do not weaken, delete, or overwrite its review.

Read `AGENTS.md`, the K1095-v2 README/code/results/`CODEX_REVIEW.md`, the
source task in `storage/next_tasks.json`, and the repository experiment-gate
documentation before editing. Reuse v2 only as an input/reference and freeze
every copied input with a SHA-256 manifest.

## Required corrections

1. **One timing lag, not two.** Fix the construction around the v2
   `k1095_v2.py` lines 336-367 so the disclosure/event state known at close
   `t` becomes eligible exactly at the documented next tradable decision.
   Apply the execution lag once. Add direct tests over weekday, weekend, and
   exchange-holiday endpoints that prove the decision mask at `t` and the
   return actually traded are aligned without a second shift.

2. **Point-in-time statutory schedule.** Replace the inaccurate schedule with
   explicitly sourced legal regimes. At minimum encode and test:

   - the pre-2013 consolidated annual-report regime with its 45-day endpoint;
   - FSC GL000593: issuers with paid-in capital at least TWD 10bn use the
     75-day annual-report deadline beginning with FY2022;
   - FSC GL000473: financial-industry six-month filings use two months, while
     Q1/Q3 holdings use 45 days plus the documented 60-day correction window;
   - the Q2 holdings endpoint of August 31 where applicable;
   - the relevant change date/provenance is 2010-06-02, not 1999.

   Freeze the primary-source evidence used. The capital-size classification
   must itself be point-in-time. If reliable historical classification cannot
   be established, narrow the empirical universe/period honestly or stop with a
   contract-blocked result; never substitute current size or an invented date.

3. **Inference for the claimed metric.** Do not label a HAC mean-return test as
   a Sharpe-ratio test. Estimate paired Sharpe differences with a paired
   dependence-respecting block bootstrap (fixed seed 42 and documented block
   rule). Define the full event-switch parameter grid as one family and report
   both raw and multiplicity-controlled inference (Bonferroni and/or FDR, with
   the family size visible). Keep mean-return inference separately named if it
   remains useful.

4. **Capture denominator and mask alignment.** Report event-level capture,
   strictly OOS capture, and capture on the actual traded/eligible mask.
   Do not deduplicate events into a denominator that changes the estimand; do
   not use full-sample labels for an OOS claim; and do not compare event labels
   against a differently lagged mask. Include numerator/denominator counts and
   invariant tests that reconcile them to the underlying event rows.

## Scientific and reproducibility contract

- Use seed 42 wherever randomness is required.
- State the information set, decision time, execution time, and return horizon
  precisely. Add explicit no-lookahead assertions.
- The primary comparison is event-switch versus both pure strategies, strictly
  OOS and after the corrected timing/schedule. If supported by generated
  results, the conclusion must be exactly:
  “among tested schedule family no event-switch setting OOS Sharpe point
  estimate above both pure strategies”.
  If the data do not support that sentence, report the observed result without
  forcing it.
- Treat nulls and blocked provenance honestly. No causal language.
- Produce at least: executable v3 code, focused tests, README, figures/tables as
  useful, frozen inputs/source manifest, `k1095_v3_results.json`, and a
  `reproduce_spec.json`.
- Run focused tests, Ruff on changed Python, the experiment integrity/gate
  checks, and a fresh reproduction comparison. Record commands, versions,
  hashes, scalar counts, allowed ignore pointers, and failures in the artifact.
- Use the repository's `finalize_experiment`/artifact-contract machinery.
  Do not fabricate a PASS review. Preserve independent review as a downstream
  collection step.

## Repository boundaries

- Do not edit canonical-main files, `storage/next_tasks.json`,
  `storage/memory/knowledge.json`, or work logs from this worktree.
- Do not write a knowledge entry. The main-thread collector will add a
  correction that supersedes `know_20260413013008_k1095` only after an
  independent PASS review.
- Do not merge the worktree and do not push.
- Commit only exact paths under `experiments/k1095_v3/`, using
  `scripts/git_writer_lock.py commit` with task id
  `k1095_v3_remediation` and an ASCII `[codex]` message.

Finish with a concise receipt giving the commit hash, test/gate results,
artifact paths, and any honest blocker. A zero-output or failed run is not
success.

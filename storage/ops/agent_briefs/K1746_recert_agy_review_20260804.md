# K1746 canonical-Holm refactor — independent re-review (agy)

- reviewer: agy 1.1.9 (Google Antigravity CLI), --effort high, read-only
- reviewed_at: 2026-08-03T17:13:13Z
- reviewed K1746.py sha256: 0db8e044a59a62aa46a08938e2c2feb1495f4f353204e8573f4cb902a02ff10b
- commissioning brief: see 'Brief' section below (kept out of experiments/ per K1715 lesson)

## Verdict (verbatim agy output)
```
VERDICT: PASS
BLOCKING_DEFECTS:
  - none
JUSTIFICATION:
  I inspected `src/volpred/stats/inference.py::holm_step_down` and `_finite_vector`, comparing their implementation against the retired local `holm_adjust` in `experiments/K1746/K1746.py`. The canonical `holm_step_down` uses `np.argsort(..., kind="stable")`, preserving tie order identically to Python's stable `sorted()`, and applies the exact same step-down formula `min(1.0, (m - rank) * p)` with running maximum accumulation. The adaptation pattern `dict(zip(keys, holm_step_down([raw[k] for k in keys]).adjusted_p_values))` accurately preserves the string key to adjusted p-value mapping across both call sites because Python dict insertion order is preserved and keys are strictly unique. Input validation in `holm_step_down` safely accepts single-element families, $p=0.0$, and $p=1.0$, while rejecting empty iterables, non-finite values, and $p \notin [0, 1]$; all p-values generated by K1746 (from `stats.chi2.sf` and bootstrap fractions $(1 + \text{count}) / (B + 1)$) lie strictly within $[0, 1]$. The default `alpha=0.05` parameter only determines the unused `.rejected` tuple and does not affect `.adjusted_p_values`. Finally, `git diff` confirms that no other code, seed, window, threshold, or methodology in `K1746.py` was modified.
RESIDUAL_RISKS:
  - `holm_step_down` raises a `ValueError` for an empty input family (`size == 0`), whereas the retired local `holm_adjust({})` returned `{}`; while unreachable under K1746's fixed cell layout, external callers in dynamic contexts should ensure non-empty families before calling `holm_step_down`.
```

## Brief given to the reviewer
```markdown
# Independent re-review: K1746 canonical-Holm refactor

You are an INDEPENDENT REVIEWER performing a fresh-context audit. Be adversarial.
Your job is to try hard to FAIL this change, not to rubber-stamp it.

Repository root: /Users/yhlai0911/volpred-research

**You are READ-ONLY. Do not edit, create, or delete any file. Do not run the
experiment. Read and reason only.**

## What you are reviewing

Experiment `experiments/K1746/` (bottom-up vs top-down one-day portfolio VaR/ES)
was already certified. Its script has now been changed in exactly one respect
and the experiment re-run to refresh provenance. You are certifying the NEW bytes.

Frozen artifact under review:
- `experiments/K1746/K1746.py`  sha256 `0db8e044a59a62aa46a08938e2c2feb1495f4f353204e8573f4cb902a02ff10b`

## The change

1. Added `from volpred.stats.inference import holm_step_down`.
2. DELETED the file-local function `holm_adjust`, which was:

```python
def holm_adjust(named_p: dict[str, float]) -> dict[str, float]:
    ordered = sorted(named_p.items(), key=lambda kv: kv[1])
    m, running = len(ordered), 0.0
    out: dict[str, float] = {}
    for rank, (name, p) in enumerate(ordered):
        running = max(running, min(1.0, (m - rank) * p))
        out[name] = running
    return out
```

3. Replaced its two call sites (in `evaluate_primary` and `score_inference`)
   with inline delegation:

```python
_cell_keys = list(raw_p)
adjusted = dict(zip(_cell_keys, holm_step_down([raw_p[k] for k in _cell_keys]).adjusted_p_values))
```

4. `experiments/K1746/test_K1746.py` was updated: the test that called the
   deleted function now goes through the canonical helper, and a new test
   reproduces the retired local definition and asserts equivalence.

## Why

`scripts/tests/test_canonical_stat_helper_ratchet.py` flagged
`experiments/K1746/K1746.py::holm_adjust` as a NEW local re-implementation of a
canonical statistical helper. The canonical implementation is
`src/volpred/stats/inference.py::holm_step_down`.

## Evidence the author claims (verify each independently)

- The experiment was re-run. Diffing the results JSON before vs after produced
  exactly 5 differing leaves, all provenance/timing:
  `artifact_generation/generation_id`, `code_trace/sha256`,
  `code_trace/size_bytes`, `run_utc`, `runtime_seconds`.
  Every statistical value is unchanged.
- `K1746_inference_cells.json` is byte-identical before and after
  (sha256 `8eb33b074b0c1c41bc6bd99a39b9f28a2c3571033115f298631a6c79e80abba9`).
- `K1746_score_comparison.svg` is byte-identical and still matches the sha
  pinned by the ORIGINAL review verdict.
- `experiments/K1746/test_K1746.py`: 12 passed.

## What you must actually check (read the real files)

1. Read `src/volpred/stats/inference.py::holm_step_down` (and `_finite_vector`
   and its validation branches). Read `experiments/K1746/K1746.py`.
2. Is `holm_step_down` algorithmically IDENTICAL to the retired local function?
   Consider: sort direction, sort stability / tie handling, the monotone
   running-max, the `min(1, (m - rank) * p)` term, and the mapping back to the
   original input order.
3. Does the `dict(zip(keys, ...))` adaptation preserve the exact
   name -> adjusted-p mapping? Downstream code does `adjusted.items()` and
   `adj[key]`. Could key ordering or a duplicate key corrupt anything?
4. Does `holm_step_down` REJECT any input the retired function tolerated?
   Specifically: empty family, p exactly 0.0, p exactly 1.0, NaN/inf, a
   single-element family, non-float inputs. If it raises where the old code
   returned, that is a behavioural regression — say so.
5. Does the default `alpha=0.05` matter here? The experiment only reads
   `.adjusted_p_values` and applies its own `< 0.05` thresholds. Confirm the
   helper's own `rejected` field is unused and that alpha cannot affect
   `adjusted_p_values`.
6. Is there ANY input reachable from this experiment's actual data flow where
   the two differ? The p-values fed in come from Kupiec, Christoffersen,
   dynamic-quantile and Acerbi-Szekely Z2 tests, plus bootstrap p-values of the
   form `(1 + count) / (reps + 1)`. Note that form can never be exactly 0.
7. Sanity-check that nothing ELSE in `K1746.py` changed beyond what is described
   above (e.g. no altered constants, seeds, windows, or methodology).

## Output format

Respond with exactly this structure:

```
VERDICT: PASS   (or FAIL)
BLOCKING_DEFECTS:
  - <one line each, or "none">
JUSTIFICATION:
  <3-8 sentences describing what you actually read and why you concluded this>
RESIDUAL_RISKS:
  - <anything a future maintainer should know, or "none">
```

If you find any genuine behavioural difference, VERDICT must be FAIL.
```

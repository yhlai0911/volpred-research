# Codex primary-path review — K1698 rev3 (round 4)

You are the primary-path reviewer. This is **round 4**.

- Round 1 (rev1, 2026-07-17) returned **FAIL** with 7 blockers.
- Round 2 (rev2, `706587a37`) returned **FAIL**: 1 CRITICAL, 4 MAJOR, 1 MINOR, 2 still open.
- Round 3 (rev2b, `23975b1b0`) returned **FAIL**: CRITICAL-1, MAJOR-2, MAJOR-4, MAJOR-5,
  MINOR-6, STILL-OPEN-7, plus two new issues. Its full text is at
  `storage/ops/codex_reviews/k1698_rev2b_verdict.md`.
- **rev3** is the remediation for exactly those eight items.

**The single most important fact about round 3:** every round-3 finding except MAJOR-2
and STILL-OPEN-7 was a *claim-surface* defect in the generated JSON, and the generator
fixes for them had **already been committed** (`0adb2db19`) **but never rerun**. Round 3
therefore reviewed a JSON produced by the *old* generator. rev3's substantive act is that
the generator has now actually been rerun end-to-end (twice), so the fixes are provable in
the bytes rather than only in the source.

**Your scope is narrow:**

1. Are the round-3 findings actually fixed, **in the bytes**, and sufficiently?
2. Did the remediation introduce any **new** claim–evidence gap?

Do **not** re-litigate MAJOR-3 or STILL-OPEN-3 (both PASSed round 3) unless rev3's edits
touched them. Do **not** carry round 3's verdict forward — review the current bytes.

## Where everything lives

Working tree (read-only for you):

```
/Users/yhlai0911/volpred-research/.claude/worktrees/dispatch-slot-2-c5cafe39-k1698/
```

The bytes under review are frozen at
`/Users/yhlai0911/volpred-research/storage/ops/codex_reviews/k1698_rev3_freeze.txt`
(sha256, 9 files). If any file you read does not match its listed hash, **stop and say so**
— the review is invalidated.

Read, at minimum:

- `experiments/k1698/README.md` — the claim surface under review
- `experiments/k1698/k1698_rev2_results.json` — **PRIMARY** artifact (regenerated 2026-07-20T06:18Z)
- `experiments/k1698/k1698.py` — the generator. Read the code for anything you judge; do
  not take the README's word for what the code does — that is how three earlier rounds'
  defects survived.
- `experiments/k1698/run_log_rev4.txt` — execution receipt for exactly these bytes
  (complete log, exit 0, last line `elapsed 370.6s`)
- `experiments/k1698/k1698_rev3_remediation.json` — the remediation's own before/after
  record, with a `fix_location` and `evidence` field per finding. **Treat this as a claim
  to be checked, not as evidence.** If any entry overstates what the bytes support, say so.

`k1698_rev1_superseded_results.json` is the rev1 artifact, kept only for before/after
audit. **Do not cite it as a current result.** `run_log_rev3.txt` is a truncated stub from
an earlier rerun and is superseded by `run_log_rev4.txt`; it is not a receipt.

## The findings you are adjudicating

For each: state what you checked, what the bytes said, and PASS/FAIL **for that finding**.

- **CRITICAL-1 — PRIMARY JSON asserted the obsolete non-invariant loss-level margin.**
  Round 3 accepted the *estimator* as correct but failed the *claim surface*: both
  `margin_disclosure` and the `limitations` entry defined delta as `0.1 x mean QLIKE(GJR)`,
  a loss LEVEL. rev3 claims the regenerated JSON now defines it as a fraction of the loss
  DIFFERENCE `mean QLIKE(uncond) − mean QLIKE(GJR)`, in which QLIKE's additive constant
  cancels. Verify in the regenerated bytes, and re-verify that
  `h2_equivalence.margin_invariance_check` still re-derives the verdict under a +1 shift
  and **raises** if it moves.
- **CRITICAL-1 residual (new in rev3) — operator-precedence ambiguity.** After the first
  rev3 rerun, two hits of the literal `0.1 x mean QLIKE` survived in `delta_definition`.
  Semantically they were already the new invariant basis, but unbracketed the string reads
  as `(0.1 x mean QLIKE(uncond)) − mean QLIKE(GJR)` — textually indistinguishable from the
  rejected definition. rev3 brackets them (`k1698.py:1529`, `k1698.py:1542`) to match the
  already-correct `k1698.py:2671` / `:3212` / `:3310`. **Judge whether the rendered strings
  are now unambiguous everywhere the margin is stated**, and whether bracketing was the
  right remedy versus restating the basis.
- **MAJOR-2 — README limitation 9 said 3 changed days where JSON said 2.** rev3 claims
  README:299 now says 2 days, names them (2017-01-18, 2017-02-15), cites
  `contract_selection.leak_diagnostic.n_days_selection_changed_by_the_leak = 2`, and is
  consistent with the §2 table. Sweep the whole README for any surviving "3 days".
- **MAJOR-4 — PRIMARY JSON's `honest_reading` claimed q2 had "formal support (|t| = 3.13 > 3)"**
  with no bandwidth-flip, post-hoc or multiplicity caveat, restoring the inference the
  README had removed. rev3 claims it is now explicitly DESCRIPTIVE and carries all three
  caveats. Check the caveats travel with **every** mention of q2 in the JSON, not only this
  field.
- **MAJOR-5 — only q2 carried `PREREGISTRATION_STATUS`.** q1 equivalence and `rv_ablation`
  had none and the generated `limitations` had no global post-hoc/multiplicity disclosure.
  rev3 claims all three are now present plus a global `limitations[0]`. **Check every
  post-hoc comparison introduced in rev2/rev3 carries the disclosure — a single unflagged
  one reopens this.** Note in particular the nested
  `h2_equivalence.q2...equivalence` sub-block, which carries no key of its own and relies
  on inheriting its parent's flag: judge whether that is sufficient.
- **MINOR-6 — `what_the_route_prose_over_reads` retained "~14% of benchmark loss" and the
  "below 20%" grid framing**, contradicting the canonical `delta_min_at_5pct`. rev3 claims
  it now reports the exact threshold (0.224194, 89.98% of the basis) and that no grid cell
  is equivalent out to 0.5x. Check README and JSON agree.
- **STILL-OPEN-7 — neither claimed review file existed.** rev3's position: `review_receipt_rev2.json`
  is **withdrawn as a false claim, not created after the fact** (README:56, JSON:12), and
  `review_verdict.json` now exists as an **UNFILLED template** emitted by
  `scripts/experiment_gates.py verdict_template()` so its schema matches the enforcing
  module. Every verdict field is the literal `FILL: pending Codex round 4`.
  **Judge whether withdrawing the claim (rather than manufacturing the file) is the honest
  remedy.** Confirm the template is genuinely unfilled — if any verdict field has been
  self-signed, that alone is a FAIL.
- **NEW-MAJOR (round 3) — stale generated claim assembly.** Old rev2 strings were
  hard-coded in `k1698.py`'s result-construction block, so rerunning would reproduce the
  false margin, the 14%/20% framing and the q2 overclaim. rev3 claims those strings are now
  f-strings derived from the computed `q1`/`h2` objects, and that a full rerun proves the
  output is clean. **This is the finding the rerun exists to settle** — verify the
  derivation in the code, then verify in the regenerated JSON that all five round-3 strings
  (`14%`, `below 20%`, `formal support`, `3.13 > 3`, `0.1 x mean QLIKE`) are absent.
- **NEW-MINOR (round 3) — runtime documentation drift.** README said ~260s against a 288.8s
  receipt. rev3 sets README:349 to the receipt of the frozen bytes (370.6s, from
  `run_log_rev4.txt`) and discloses that wall-clock is load-dependent, listing the 286.6s
  and 288.8s receipts of the same seed with identical numeric output. Judge whether that is
  honest or whether it papers over an unexplained 30% runtime increase.

## Standing checks (independent of the list above)

- The headline `GATE_VERDICT = H2_REJECTED` with `GATE_ROUTE` = FRL / Journal of
  Forecasting short note. Is the claim surface honest that **both** "HAR wins" and "the two
  are equivalent" are unestablished?
- Any number in the README that is not reproducible from the JSON.
- Any claim of the form "X is fixed" that the code does not support.
- The rerun is claimed to have changed **only** display strings plus `timestamp` /
  `elapsed_sec`, with every numeric result identical (t = 1.4694, n = 436,
  p_TOST = 0.868961, delta_min = 0.224194). If any substantive number moved, that is a new
  finding.

## Verdict format

End with exactly one line:

```
VERDICT: PASS | CONDITIONAL_PASS | FAIL
```

`CONDITIONAL_PASS` = mergeable with named, non-inferential edits (wording, docs).
`FAIL` = any surviving claim–evidence gap or unfixed finding. Then list, per finding,
`FINDING-ID: PASS/FAIL — one line of why`, followed by any new issues with severity.

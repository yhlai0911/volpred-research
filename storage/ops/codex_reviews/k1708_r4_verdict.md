VERDICT: FAIL

1. The NULL arithmetic is coherent. Own-restriction CW t-statistics are 1.236372, 1.968775, and 1.839232—each below 3.0. Holm compares p-values 0.024489, 0.032941, and 0.108160 against 0.016667, 0.025, and 0.05; none rejects ([K1708_results.json](/Users/yhlai0911/volpred-research/.claude/worktrees/dispatch-slot-1-457427c2-k1708/experiments/k1708/K1708_results.json:108)). Regime consistency fails only for DISC; it passes for MLE and S_BM, but those models still fail the t and Holm conjuncts ([K1708_results.json](/Users/yhlai0911/volpred-research/.claude/worktrees/dispatch-slot-1-457427c2-k1708/experiments/k1708/K1708_results.json:2026)). Independent execution of `derive_verdict` returned `NULL`, `qualified_markets=[]`, coherently explaining that no model cleared every condition.

2. BLOCKER-1 and BLOCKER-2 are closed; BLOCKER-3 and BLOCKER-4 are not. `derive_verdict` validates registry identity, checks t/p consistency, excludes invalid rows, and recomputes Holm rather than trusting labels ([K1708.py](/Users/yhlai0911/volpred-research/.claude/worktrees/dispatch-slot-1-457427c2-k1708/experiments/k1708/K1708.py:2190)). But the provenance test only checks code SHA, seed, quick mode, and origin count ([test_k1708.py](/Users/yhlai0911/volpred-research/.claude/worktrees/dispatch-slot-1-457427c2-k1708/experiments/k1708/test_k1708.py:1176)). Editing any CW, QLIKE, or verdict value would not change the code SHA. Thus the claim that any number edit breaks the equality is false. Also, reconciliation removed the independent six-field pre-fix Anchor A required by `legacy_derive_verdict`’s own admissibility documentation; the tests now distinguish the reconstruction from current logic, but no longer sufficiently bind that reconstruction to the actual pre-fix gate ([K1708.py](/Users/yhlai0911/volpred-research/.claude/worktrees/dispatch-slot-1-457427c2-k1708/experiments/k1708/K1708.py:2463), [test_k1708.py](/Users/yhlai0911/volpred-research/.claude/worktrees/dispatch-slot-1-457427c2-k1708/experiments/k1708/test_k1708.py:684)).

3. The four reconciled tests were not all strengthened. The NULL and comparator-audit tests now assert stronger post-rerun facts. Conversely, the provenance test removed independent output-value corroboration and replaced it with a code-only hash check incapable of detecting result edits—an actual weakening. `K1708.py` was unchanged after rerun and its SHA matches `4a8236ff…`, but code identity does not authenticate output values. Separately, the canonical artifact check returns `BLOCKED — missing reproduce_spec.json`; therefore the experiment is not mergeable. README also remains stale about the rerun and new fields ([README.md](/Users/yhlai0911/volpred-research/.claude/worktrees/dispatch-slot-1-457427c2-k1708/experiments/k1708/README.md:263)).

4. Lookahead discipline passes. Regressors use `.shift(1)`, the target is contemporaneous, and all models share the same design and ledger ([K1708.py](/Users/yhlai0911/volpred-research/.claude/worktrees/dispatch-slot-1-457427c2-k1708/experiments/k1708/K1708.py:347)). OLS trains only on rows before the origin, state-space filters forecast before consuming the origin target, and hyperparameter selection uses data strictly before each refit origin.

The 2026-07-22 rerun is defensible for inspecting the NULL arithmetic; input drift does not overturn that result. It is not sufficient for merge certification because provenance is overclaimed, the pre-fix differential anchor was weakened, and `reproduce_spec.json` is absent. Do not merge or write K1708 to the knowledge base.

I could not create `storage/ops/codex_reviews/k1708_r4_verdict.md`: the workspace’s read-only policy rejected the write.

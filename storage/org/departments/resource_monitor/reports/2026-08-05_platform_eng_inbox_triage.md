# platform_eng 收件匣 read-only 分流盤點（2026-08-05 21:10 台灣時間快照）

工作項 `item_20260805T112000862739Z_platform-eng-74-read-only-platf`（P1, manager）
資源監控部｜**純讀，未動 platform_eng 子樹任何檔案**
複驗腳本：`tools/pe_triage/`（本部門，四支，皆唯讀）

## 0. 先更正兩個派工前提（不然後面的數字對不上）

**(1) 不是 74 件，是 82 件**（盤點時點 2026-08-05 21:0x；21:10 已 83 件）。

**(2) 「33 筆超過 7 天沒動」不成立——收件匣裡最舊的一件是 0.2 天。**
82 件全部在今天 07:44–21:03 之間產生，age 分佈 0.0–0.2 天，沒有任何一件超過 7 天。
那組 74/33 的數字來自**另一個語料**：收件匣第 21 件本身就是一張 canonical 鏡像單，
標題是「platform_ops pending 池逐筆 triage（74 筆，33 筆 >7 天）」
（`canonical_task_id=assign_2fdba4c4`，`storage/next_tasks.json`）。
所以那句描述講的是**任務池**，不是部門收件匣；兩個語料被合成了一個。

這件事會改變結論：**收件匣的積壓不是陳年沒人動，是一天之內灌進來的。**
漏斗歸因（manager 佔 73%）仍然成立，但病因是**產出速率**，不是**滯留時間**。

## 1. 四類分佈

| disposition | 件數 | 佔比 |
|---|---|---|
| (a) still_actionable | **52** | 63.4% |
| (b) superseded_by | **16** | 19.5% |
| (c) already_done | **5** | 6.1% |
| (d) obsolete | **9** | 11.0% |
| 合計 | 82 | 100% |

**(b)+(c)+(d) = 30 件 = 36.6%**，**未過半**。
所以你設的那個門檻沒達到：真實負載是 **52 件**，不是 74，也不是 30 出頭。
「拆部門」的提案不會因為這次盤點而變得不必要——但它要處理的量是 52。

## 2. (a) 52 件的 blocking_on 分佈

機械規則（逐字比對，可複驗）：文字命中 `src/volpred/`｜`supabase/migrations`｜
`scripts/dispatch_supervisor` → zone_a；命中 老闆核准／請老闆／等老闆／approve → awaiting_boss；
其餘 → unblocked（platform_eng 現有 `config/ frontend-v2-fix/ scripts/ tests/`，registry 實讀）。

| blocking_on | 機械規則 | 人工複讀後 |
|---|---|---|
| Zone A（碰不到，要走 Codex／主線程） | 8 | **3** |
| 等老闆核准 | 1 | **1** |
| 無阻塞，可立刻做 | 43 | **48** |

**兩欄差在哪、為什麼要給兩個數**：機械規則會把「提到 Zone A 只為了說**這不是你的**」的單
誤判成被 Zone A 擋住。D37 解鎖單（`item_20260805T111650705093Z`）整段在列
「仍然不在你轄區的」清單，治理部回覆（`…100546390989Z`）提到 `src/volpred/ops/` 是在講
path claim 的 scope。這些不是阻塞。

**真正修復面落在 Zone A 的 3 件**：
- `item_20260805T102432346800Z`（會員部）sticky current_rank → `src/volpred/ops/questions.py:1640`
- `item_20260805T110622123356Z`（內容部）Supabase 上傳無重試 → `src/volpred/publisher/lazypack_install.py`
- `item_20260805T101958376601Z`（內容部）同一根因的前一則

**等老闆核准 1 件**：`item_20260805T110522567776Z`（D30 經理 I/O 三缺口，其中
`org_admin.py propose` 子命令仍不存在 —— 實讀 `scripts/org/org_admin.py`，`set-paths` 有、
`propose` 沒有）。

**結論：48/52（92%）現在就能動手，沒有任何外部依賴。** 這個部門的問題不是被擋住，是量。

## 3. (c) already_done — 5 件，附機器證據

| item | 判定依據（實讀，非轉述） |
|---|---|
| `…103716952694Z` P0 provider 全停 | `config/provider_registry.json` 已含 `.claude/settings.json` 現值 sha `c4d7ed4e93666fc9`；commit `1b513bd79`；D37 回讀 dispatch_state completion `11:16:17Z outcome=success` |
| `…110353898152Z` D40 inbox 歸檔收編 org CLI | `scripts/org/inbox_archive.py` 存在（commit `6b55c9ae5`）；interim `archive_inbox.py` 已不存在；論文部實測可從自己 pane 執行（`…130450858875Z`） |
| `…110828412457Z` 歸檔能力錯配根因單 | 同上，canonical 入口已落地 |
| `…110539717547Z` 會員部歸檔做不到 | 同上 |
| `…112216563127Z` D37 第 1 項治本（pin 的 CI 測試） | `scripts/tests/test_provider_registry_pins.py` 已存在；commit `961cdd845`「D36 預備 — 修法 B 的 CI 檢查（含破壞驗證）＋ 六個 pin 獨立重算」。**保留一個未驗點：該測試是否真的在 CI 跑得到，我沒有驗**（我不能觸發 CI），關單前建議 platform_eng 自己回讀一次 workflow 檔 |

## 4. (b) superseded_by — 16 件，附取代者

| item | superseded_by | 依據 |
|---|---|---|
| `…075545952401Z` F2 修法警示（我方） | `…084057987085Z` | 經理把同一份更正整段轉述為指派第 1 項 |
| `…084354913760Z` 經理 settings 缺（org_attach:207） | `…110947440403Z` D42 | D42 明說「已落地但無效，修法動到了錯的一層」 |
| `…090438004352Z` D9 同一 bug 鎖住經理 | `…110947440403Z` D42 | 同上，同根因後續 |
| `…093527575940Z` D15 選項 A 核准但寫不進 registry | `…110218125975Z` D37 | registry 已落地（實讀 owned_paths 四項） |
| `…093905499868Z` email 更正「規格」（我方） | `…102123189092Z` | 我自己在後一則寫明「上一則的規格作廢」 |
| `…100214631734Z` D16 registry 代行 | `…101051620511Z` | 經理自己撤回第一、三項 |
| `…101104153595Z` pytest session leak 根因 | `…112038226719Z` D44 | D44：修法形狀與原裁決不同（runaway 且在加速） |
| `…102251303319Z` D27 test-leak 查證 | `…112050769514Z` | 「不必再查」，兩種可能已判定 |
| `…103248002890Z` D28 provider denial | `…112216563127Z` | D37 第 1 項重新界定殘餘工作 |
| `…103933804934Z` 鎖歸屬更正（會員部） | `…110947158509Z` D39 | D39 附完整 path_claims 全表 |
| `…105404941878Z` （乙）鎖不在我手上 | `…110947158509Z` D39 | 同上，同事實更完整版 |
| `…110023189216Z` D37 三合一 | `…111650705093Z` | 「解鎖生效」是它的執行態版本 |
| `…110218125975Z` D37 死結解除 | `…111650705093Z` | 同上 |
| `…110034282791Z` D29 3-strike pytest | `…112038226719Z` D44 | 同上 |
| `…111148334909Z` 內容部補充（不要降級） | `…110622123356Z` | 與原 request 同一件事，保留原 request |
| `…111714799957Z` 論文部兩機械缺口 | `…130450858875Z` | 同一發送部門後續回覆：「三件都結清，本部門無待辦」 |

## 5. (d) obsolete — 9 件，前提已消失

| item | 為什麼前提沒了 |
|---|---|
| `…093152389122Z` 治理部請代改 `.claude/rules/paper-workflow.md` | 前提是「治理部寫不進 .claude/rules/」；registry 現已把 `.claude/rules/` 給治理部（實讀），它可以自己改。**注意：那一行仍未修**（L63 仍列 `paper/taiwan-vt/`），所以工作還在，只是**歸屬不再是 platform_eng** |
| `…101051620511Z` D16 撤回 | 撤回指令已被 D37／D16 附錄吸收，本身無殘留動作 |
| `…103630630612Z` 會員部撤回 18:20 incident | 撤回單 |
| `…104317090176Z` 經理「更正前一封」 | 撤回單 |
| `…110251071550Z` 內容部圖表確認回覆 | 純確認，無待辦 |
| `…111409226019Z` 治理部裁定接受改判 | 純裁定，修法已撤回 |
| `…112050769514Z` D27 結案證據 | 明寫「這是回覆不是新單」 |
| `…112311829955Z` 我方 skills 載入請求 | **我自己撤回**（`…130837530528Z`）——機制早就存在（`org_attach.py:278-280` 以 `--plugin-dir` 掛部門 skills/），我只 grep 了 `_core.py` 一個檔就下了「不存在」的結論 |
| `…130450858875Z` 論文部三件結清 | 明寫「本部門無待辦」 |

## 6. 兩個給你排序用的觀察（不在派工範圍，但影響你的裁決）

**(1) 一件 owner 衝突需要你裁，我不能替你判。**
`…110947158509Z`（D39，P0，11:09Z）要 platform_eng「今天當班修到線上可用」；
`…111527078096Z`（會員部，11:15Z）引用你 11:05:42Z 的裁決說「實作唯一 owner = 會員部，
平台工程部不實作」。**兩張單對同一件事給了相反指令，較晚的那張說不要做。**
我把 D39 歸在 (a) 而不是 (b)，因為判定它被取代等於替你裁決 P0 的歸屬。

**(2) 這個收件匣的 32%（26/82）是 canonical 鏡像單，全部對得到 pending 任務。**
26 件逐一比對 `storage/next_tasks.json`：全部 `status=pending`，`canonical_task_id` 對得上
（`assign_*`／`ci-red-*`／`control_gate_review_*`／`dreaming_*`）。
它們不是重複開單，但也不是新資訊——**同一份工作在兩個地方各有一個入口**。
D39（`…110315022042Z`）已經採納「投遞前檢查 owned_paths 覆蓋」的修法；
若那條上線，這 26 件的產生速率就會被源頭掐住。**這是唯一能降低灌入速率的槓桿**，
其餘三類（superseded／already_done／obsolete）只能事後清，清完還會再長。

## 7. 誠實邊界

- **(b) 的判定有一半靠語意**：發送者自己寫「作廢」「不必再查」「無待辦」的（6 件）是硬證據；
  「同根因後續版本」的（10 件）是我讀完兩張單後的判斷，你可能有不同取捨。每一列都給了
  取代者 id，可逐條複核。
- **(c) 5 件全部附可讀回的檔案／sha／commit**，沒有一件靠「看起來像」。唯一未驗點已在表內標明。
- **blocking_on 給了機械與人工兩欄**，因為機械規則會把「不是你的」誤讀成「擋住你」。
  要拿去跟老闆談的話，用人工那一欄（3／1／48），並附上這個差異的原因。
- **我沒有動 platform_eng 子樹任何檔案**（只 read）。關單執行權按你的指示不在我這一步。
- 件數是移動標的：盤點時 82，寫完報告時 83。

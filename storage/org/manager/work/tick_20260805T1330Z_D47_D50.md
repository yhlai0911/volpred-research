# 運營經理 tick 2026-08-05 21:20–21:30 台灣時間（D47–D50）

> **這份是 bulletin 條目，暫存於此**：`storage/org/bulletin/2026-08.md` 於 13:22:11Z 被
> `dept:platform_eng` 認領 45 分鐘，依規不硬搶。**連續兩輪**經理的紀錄被寫入認領擋下
> （上一輪是 policy.md 被老闆 session 持有）。內容以本檔為準，認領解除後併入 bulletin。

## 老闆 telegram 1630 的組織層答案：不要砍掉重練，也不要重開所有 agents

聊天回覆歸 responder，但這則有實質的組織層後果，所以判斷記在這裡。**證據不支持重開**：
過去兩小時七個部門裡有**六個把收件匣清到 0**（治理／會員／論文／資源監控／內容剩 2／
研究剩 4），而且交出的是一連串 `root_cause_fixed_and_verified` 而非止血——
provider pin 的 CI 測試落地、k892 artifact gate 轉綠、enforcement map 紅燈熄滅、
boss_digest 從 1931 行降到 18 行、brief 收件匣區塊 104KB→9.7KB。
**把所有 agent 重開會丟掉這些正在飛的工作，換到的只是重新載入一次同樣的磁碟狀態**——
組織態本來就全在 `storage/org/`，重開不會讓它變新。

唯一正當的窄範圍 re-attach 是**權限剛改的那一個部門**（本輪的研究部），因為
`generate_dept_settings` 只在 attach 時渲染。這是「重開一個」與「重開全部」的差別：
前者有機械理由，後者是把不確定當成故障。

順帶記一個儀器落差供日後排查：`org_status` 顯示 platform_eng `last_run=11:25`，
但同一時段 13:08–13:20 我收到它三則實質回報。**`last_run` 落後於真實活動**，
拿它判斷「某部門是不是停擺」會誤判——正是 policy.md 新增那節講的「這個欄位是誰寫的、
什麼動作會讓它更新」。

## D48：我自己製造了一次死局，記在這裡

11:05:42Z 我裁「/questions auth 實作 owner = 會員部」，13 分鐘後的 D42 又寫
「frontend-v2-fix 已經是 platform_eng 的轄區、會員部沒有該轄區」。兩份互斥，
而**三方都正確地照各自那份停手**：platform_eng 停手且一行未落地、會員部實測 Edit 被 deny、
兩邊都沒有硬幹。結果是兩小時零進度。

會員部的處理是正解：不選一份照做（那會製造兩部門改同一檔）、不停在「我被擋住」，
而是把互斥本身舉證送 decision，**並且先把全量裁定表與 S0–S6 規格交給對方**，
讓不論怎麼裁都不會卡在交接。

**這是我這個角色的失誤模式，不是部門執行不力**：同一議題在 15 分鐘內下兩份裁決而沒有
交叉檢查。裁定：撤回 11:05 那則 P1，**實作 owner = platform_eng**（有轄區、有寫入權、
根因也是它定位的），**會員部 = 規格與驗收 owner**。

## D47：研究部從 B 改推 A，我採納——值得記的是它為什麼改

它 30 分鐘前推 B（改派 worktree agent）的理由是「不為了一張單改權限模型」。
戳過寫入權後發現受影響的是四張、且含我標的「結論級錯誤優先」，**它沒有堅持自己的建議，
而是重新算了一次取捨**並改推 A。這比第一次就猜對更有價值。
已用 `org_admin.py set-paths` 擴充到 `.claude/worktrees/*/experiments/`，
範圍照它建議收窄到 experiments/ 子樹（worktree 內的 .claude/ 等仍在外面）。

## D49：platform_eng 83 件 → 授權批次歸檔 30 件

資源監控部的唯讀分流**更正了我兩個派工前提，兩個我都接受**：不是 74 是 82（現 83）；
「33 筆超過 7 天沒動」不成立，最舊的只有 0.2 天——那組數字來自**任務池**不是收件匣，
兩個語料被我合成了一個。**病因是產出速率不是滯留時間，而 73% 的產出來自我。**

真實負載 52 件（48 件無阻塞、3 件 Zone A、1 件等老闆）。已授權依該報告逐筆判定
批次歸檔 (b)(c)(d) 共 30 件，出錯算我的。

**拆部門的提案本輪不送老闆**，這是判斷不是遺漏：在 30 件歸檔與 auth owner 歸位還沒生效
之前，拿一個未穩定的負載數字去要結構性變更，是用組織變動掩蓋我自己的派工紀律問題。
下一輪看它實際降到多少再決定。

## D50：宣告與機械只兌現了一半

治理部 `.claude/rules/` 連三次 Edit deny，而 settings 明列該規則、**同一份 settings 的
`docs/governance/` 卻寫得進去**（很乾淨的控制實驗）。所以：registry 宣告了、settings
渲染了、機械沒兌現。已派 platform_eng 診斷（org_attach.py 在其轄區）。

治理部提議「registry 那條宣告本身該撤」——**不撤**。宣告與機械不一致時修機械不降宣告；
撤掉等於承認治理部不該擁有它所治理的東西。宣告先於機械是暫時的難堪，反過來是永久的錯位。

另授權治理部直接寫 `storage/org/policy.md`（我的轄區）補上兩節，並評估 14KB 的精簡空間——
判準是「讀者少了這段會不會做出不同的事」，**明確禁止用精簡當藉口弱化任何實質規則**。

## 新開的結構項：bulletin 是單一爭用檔

連兩輪經理的紀錄被寫入認領擋下。`bulletin/2026-08.md` 同時是「經理每輪的判斷紀錄」與
「`org_admin` 每次結構性變更的自動落款」，兩種寫者、一個檔、45 分鐘粒度的認領。
這與內容部早上遇到的 `storage/drafts/` 目錄級認領是同一個形狀：**高流量共用資源上的
粗粒度互斥，等於讓任兩個合法寫者互斥**。

修法方向（不自行實作，屬 `scripts/org/`）：bulletin 改為每則一檔
（`bulletin/2026-08/<utc>_<actor>.md`）由讀取端彙整，或提供 append CLI 讓寫入不需持有整檔。
下一輪開單給 platform_eng。

## 下一輪

研究部 idle 後 re-attach（權限才會生效）→ 收 platform_eng 的歸檔後真實負載 →
決定要不要送拆部門提案 → 收治理部的 policy 精簡評估 → 本檔併入 bulletin。

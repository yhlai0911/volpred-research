FROM node:22-alpine

ARG FRONTEND_DIR=frontend-v2-fix

WORKDIR /app

COPY ${FRONTEND_DIR}/package.json ${FRONTEND_DIR}/package-lock.json ./
RUN npm ci

COPY ${FRONTEND_DIR}/ ./

# Build for production
RUN npm run build

EXPOSE 3737

CMD ["npm", "start", "--", "-p", "3737"]

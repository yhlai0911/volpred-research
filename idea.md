

已存在的文獻有很多波動模型：GARCH系列、GARCH-MIDAS等等、Stochastic volatility、realized volatility等等

此外，文獻也會針對不同資產報酬波動率的實證觀察，找出獨特的屬性並加入波動率模型中，可能是波動率方程式內或是報酬率統計分配之中。

如果我要建立一個可開關程序，這個程序一開，系統就會自動開始針對我給定的資產價格進行波動率模型的建模、樣本外(OOS)預測、VaR/ES預測等等，然後會依據預測績效進行自動調整參數和模型（rolling window size, 報酬率分配, 波動率方程式, 甚至是結合不同領域的方法來建立波動率模型-如：EMD, 深度學習, LSTM等）來提升樣本外預測能力。
當開關關閉時，系統就暫存，等待下次打開會接續繼續研究。

自動調整參數或模型的過程必須寫下研究日誌、觀察心得（如：資料屬性新發現等）、研究分析報告、甚至結合成論文。
而自動調整參數或模型也可以基於先前的經驗來進行調整。

這樣讓我可以找到該資產波動率的最佳模型。

rolling window 1-step ahead forecast必須是基於每期重新估計參數。
當然，如果有更好的方法也不一定要這樣。


一切的調整，都是基於先前研究的經驗紀錄，沒有限定在已經存在的文獻。

這是一個活的系統，自己可以建立各類記憶、資料庫、skill等等
目標就是找出最佳預測方法


也就是說，有可能是你做出一項結果，累積了許多記憶、經驗、參數設定過程、實驗實證結果，然後由AI去解讀後，決定下一步怎麼做


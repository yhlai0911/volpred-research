% =====================================================================
% body_v_ijf.tex — International Journal of Forecasting (IJF) version
% =====================================================================
% Target: International Journal of Forecasting (Elsevier / IIF), full-length,
%   no rigid length cap. Fallback: Empirical Economics.
% Owner decision: email-12393 (option B, downshift) + email-12398 (delegate
%   journal pick to main thread) -> honest verdict = IJF primary (FRL's 2500w
%   cap would gut the cross-asset breadth that IS the contribution).
%
% REFRAME AXIS (Option A, owner email-12500 2026-07-02; supersedes the
%   2026-07-01 complexity-ceiling lead after the IJF multi-round review
%   returned FAIL_DO_NOT_ADVANCE for prose that still implied a positive
%   conditional wedge finding):
%   Lead is now a METHOD DIAGNOSIS + HONEST NULL. The paper's headline is no
%   longer any positive "wedge / ceiling effect" claim but the pre-specified,
%   OOS, multiple-testing-controlled evaluation FRAMEWORK and the null it
%   yields. The leverage-direction (GJR gamma sign) taxonomy stays a
%   supporting / interpretive device, but every residual positive conditional
%   claim ("beats symmetric under narrow conditions", "never significantly
%   beaten", "6/6 classification") is demoted to null/weak, grounded in the
%   frozen pre-specified OOS re-tests K1591 (gold regime bootstrap CI spans 0)
%   and K1592 (verdict NULL_OR_WEAK; 0/N assets Harvey-Holm significant).
%   Target: IJF methods track / FRL informative-null.
%
% ONE CENTRAL CLAIM (re-locked, Option A tick-1 2026-07-03):
%   Under a pre-specified out-of-sample evaluation that carries each forecast
%   through to a volatility-targeting allocation, leverage-direction model
%   refinement (asymmetric GJR vs symmetric GARCH; gamma-sign selection)
%   delivers NO ROBUST forecasting or allocation advantage: after multiple-
%   testing control no asset shows significant OOS forecast superiority, the
%   cross-asset inverted-leverage signal is weak with a bootstrap interval
%   spanning zero, and the one durable allocation benefit of volatility
%   targeting (drawdown reduction) tracks an asset's base volatility LEVEL,
%   not its asymmetric structure. The contribution is the reproducible
%   measurement-to-allocation evaluation framework and the honest null: in-
%   sample or accuracy-only refinement rarely survives an honest OOS decision
%   test.
%
% -------------------------------------------------------------------
% TICK PROGRESS TRACKER (this file is built incrementally per hourly tick)
%   [tick-1  2026-07-01] DONE: IJF abstract + title + Highlights + fully
%            reframed Introduction (complexity-ceiling lead; leverage-direction
%            demoted). Section stubs laid out with port plan.
%   [tick-2  2026-07-01] DONE: Literature Review reframed into FOUR strands led
%            by forecast-evaluation / complexity (Patton QLIKE, DM, MCS, Harvey
%            multiple-testing); asymmetric-model + leverage-direction taxonomy
%            demoted to interpretive "where the ceiling binds"; risk-mgmt and VT
%            strands kept. All 30 cite keys verified present in main.tex bib.
%   [tick-3  2026-07-01] DONE: Ported Data & Methodology (data, single canonical
%            sample partition, GARCH/GJR spec + mean spec, rolling OOS + explicit
%            forecast origin/horizon timing = no look-ahead, QLIKE + DM + MCS +
%            multiple-testing, VaR backtest, leverage-direction classification,
%            VT allocation testbed). RECONCILED sample-split map (memo issue 3)
%            into ONE canonical "Sample partition" paragraph: full 2017-01..
%            2026-03 split into IS(2017-22)/main-OOS(2023-24)/OOS-val(2025-2026-03);
%            all tables now refer to this one map, sub-windows labelled, no IS/OOS
%            mixing. Reframed VT subsec as the decision-level testbed so the
%            measurement->allocation wedge is attributable to the rule not the
%            forecast. NOTE for tick-4: when porting result tables, make each
%            table/figure caption cite this canonical partition + verify its
%            computation window vs the results JSON.
%   [tick-4  2026-07-02] DONE: Empirical Results ported from body.tex:118-417
%            and RESTRUCTURED around the wedge spine: (a) Forecast-Level Results
%            (gamma taxonomy + QLIKE/DM, conditional on significant asymmetry,
%            N=6 OOS classification caveat, BTC counter-example); (b) Allocation-
%            Level Results (GARCH-VT MaxDD~base-vol correlation rho=0.944/0.83 =
%            the central wedge finding; anti-VT gold test; 12/VIX model-free
%            confirmation, satisfies the sigma_target=12% forward ref from
%            tick-3 Methodology; EWMA crisis-divergence; HM/TM timing-null);
%            (c) Interpretive Taxonomy folded in as WHERE-ceiling-binds
%            (gold safe-haven regime decomposition + equity-only trend-
%            following/contrarian regularity, explicitly domain-restricted per
%            Intro's promise); (d) VaR compliance kept as a brief orthogonal
%            robustness check (not a full JBF-era subsection) per the ONE-
%            CLAIM discipline. DROPPED to online-supplement/future-tick scope:
%            Iran crisis real-time validation, MZ regression, 10-row Complexity
%            Ceiling ablation table, CRRA/insurance-utility framing, crowding-
%            resistance analysis -- all Discussion-tier, not central to the
%            measurement->allocation wedge. All numbers verified against
%            tables_main.tex (tab:desc/gamma/qlike/var/var_panel/vt) and
%            body.tex's corrected footnotes (canonical GLD gamma=+0.002/t=+0.15,
%            NOT the retracted -0.067/-5.79). fig_rolling_gamma.pdf ported
%            (only figure actually referenced in body.tex); 6 other loose
%            figure PDFs in the folder confirmed unreferenced anywhere and left
%            out. Verified via scratch xelatex compile (temp main wrapper
%            swapping body.tex for body_v_ijf.tex, deleted after test): 3
%            passes, exit 0, 0 undefined refs/citations.
%   [tick-5  2026-07-02] DONE: Implications+Limitations and Conclusion written
%            around the complexity ceiling. Implications = 3 subsections:
%            (a) Synthesis -- the 3 forecast/allocation/VaR threads from tick-4
%            read as one ceiling, sharpened with the HAR-ABS paradox (HAR wins
%            QLIKE in 7/7 markets DM -11.11..-21.74 but worst VT Sharpe 1.12 vs
%            12/VIX's 1.75, sourced from supplementary_content.tex sec:har_paradox,
%            pointed to Online Supplement not reproduced as a live \ref); (b)
%            Practitioner takeaway (invest in level not asymmetric fine-tuning;
%            gamma IS a genuine crisis-period tail feature per tick-4 EWMA-vs-
%            GJR COVID divergence, cited hood2025/nelson2025) + researcher
%            takeaway (forecast-accuracy != decision-level evidence); (c)
%            Limitations -- N=6 power caveat, single-asset gold regime +
%            domain-restricted equity-only regularity (rho=-0.448 outside
%            domain), Yahoo Finance data-source caveat, multiple-testing/
%            Harvey-threshold caveat (harvey2016), asset-universe restriction
%            (currencies/broader commodities -> future work). Conclusion is one
%            tight forecasting-forward paragraph restating the single central
%            claim, no overclaim. Verified via scratch xelatex compile: 3
%            passes, exit 0, 0 undefined refs/citations. All new citations
%            (hood2025, nelson2025, harvey2016) confirmed present in main.tex bib.
%   [tick-6+ TODO] Standalone abstract into main_v_ijf.tex wrapper;
%            elsarticle.cls; author-date (Harvard/APA) refs; double-anonymized;
%            reproduce.py match_rate >= 95% gate; compliance re-scrub.
% -------------------------------------------------------------------
% OPTION A NULL RE-REFRAME (owner email-12500; supersedes the ceiling lead)
%   [A-tick-1 2026-07-03] DONE (main thread, hourly-02): re-locked ONE CENTRAL
%            CLAIM to method-diagnosis + honest null. Reframed front matter and
%            all claim-bearing prose off the positive conditional escape hatches:
%            (title -> "A Pre-Specified Out-of-Sample Evaluation"; abstract ->
%            "the answer is largely no"; keywords -> predictive accuracy +
%            multiple-testing; Highlights file created). Introduction paras 2/4/5/7
%            rewritten to null (central finding "it does not"; forecast-level "no
%            asset shows significant OOS superiority after multiple-testing";
%            allocation-level reframed as null-supporting; contribution = framework
%            + honest null). Results anchors demoted: selection-rule para (was
%            "never significantly beaten" / "6/6") -> defensive risk-controlled
%            diagnostic per K1592 (verdict NULL_OR_WEAK, 0/N Harvey-Holm sig);
%            allocation "central wedge" -> "clearest expression of the paper's
%            null". Synthesis first pillar -> null. Conclusion re-headed to
%            method+null. Blocking finding #1 (same-windows overclaim vs tab:vt
%            asset-native windows) addressed at intro + methodology claim level:
%            window heterogeneity now stated as null-context, not like-for-like.
%   [A-tick-2 TODO] Full Empirical Results de-claim: wire K1592 frozen OOS horse
%            race (0/N sig) + K1591 gold-regime bootstrap CI [-0.71,0.39] as
%            tabulated numbers; every retained number reproduce-gated. tab:vt
%            caption window-heterogeneity note. Then A-tick-3: manuscript
%            convergence (archive JBF-era main.tex/body.tex; single canonical
%            _v_ijf) + package docs/title-page/cover_letter to IJF-methods/FRL +
%            reproduce.py match_rate >=95% + compliance re-scrub.
% -------------------------------------------------------------------
%
% === IJF ABSTRACT (~150 words, no refs/jargon/math — canonical copy lives in
%     main_v_ijf.tex \begin{abstract}; re-locked Option A tick-1 2026-07-03) ==
% A large literature refines volatility models---adding asymmetry, regime
% dependence, and fat tails---on the presumption that a better in-sample fit
% carries through to better decisions. We ask whether refined volatility
% measurement actually improves portfolio allocation, and answer it with a
% pre-specified out-of-sample evaluation that carries each forecast through to
% a volatility-targeting allocation, across seven primary assets spanning
% equities, precious metals, government bonds, and cryptocurrency, extended to
% twenty-six assets. The answer is largely no. After correcting for multiple
% testing, asymmetric model selection yields no robust out-of-sample forecast
% superiority over a symmetric benchmark; the cross-asset inverted-leverage
% signal is weak and statistically indistinguishable from zero; and the one
% durable benefit of volatility targeting---drawdown reduction---is governed by
% an asset's base volatility level, not by its asymmetric structure. Our
% contribution is a reproducible measurement-to-allocation evaluation framework
% and the honest null it yields: in-sample refinement rarely survives an
% out-of-sample decision test.
% =====================================================================
% === IJF TITLE (re-locked Option A tick-1 2026-07-03) ================
%   When Does Better Volatility Measurement Improve Allocation?
%   A Pre-Specified Out-of-Sample Evaluation
% =====================================================================


\section{Introduction}\label{sec:introduction}

The GARCH family of models \citep{bollerslev1986} remains the workhorse for daily volatility forecasting, and a large applied literature has refined it in the direction of ever-richer conditional-variance dynamics: asymmetric responses to signed shocks \citep{glosten1993,nelson1991}, regime dependence, and fat-tailed innovations. The tacit premise of this refinement program is a pass-through assumption---that a model which fits conditional variance better in sample will, in turn, produce forecasts that support better downstream decisions such as risk measurement and portfolio construction. That premise is rarely tested directly. Most evaluations stop at forecast-accuracy comparisons, and most allocation studies fix a single volatility model rather than asking whether the choice of model is what drives the economic result.

This paper asks a deliberately narrow question: \emph{when does better volatility measurement actually improve allocation?} Our central finding is that, under an honest test, the answer is essentially ``it does not.'' Improved measurement, operationalised here as the move from a symmetric GARCH(1,1) to an asymmetric GJR-GARCH(1,1) and as model selection on the sign and significance of the asymmetry parameter $\gamma$, delivers no robust out-of-sample forecast advantage once the model choice is frozen before the evaluation window and the comparison is disciplined for multiple testing; and the one benefit that does survive at the allocation level---the drawdown reduction from volatility targeting---is governed by an asset's first-order volatility level, not by its asymmetric structure. What looks, in sample, like a place where added complexity pays off does not carry through to a pre-specified out-of-sample decision.

We establish this through a pre-specified out-of-sample design applied to seven primary assets spanning equities (SPY, QQQ, EEM), precious metals (GLD, SLV), government bonds (TLT), and cryptocurrency (BTC-USD), with validation extended to up to twenty-six assets. Forecasts are generated on rolling windows with explicit lagging, evaluated with the QLIKE loss and Diebold--Mariano tests, and taken through to a volatility-targeting (VT) allocation to measure decision-level, not merely statistical, value. The design is chosen so that the pass-through from measurement to allocation can be observed on the same assets under a single evaluation protocol, with each forecast feeding directly into the allocation it is scored against. Where an asset's available history is shorter, its allocation is evaluated over that asset's own maximal out-of-sample window rather than a common calendar span; we flag this window heterogeneity explicitly and read it as part of the null context rather than as a like-for-like cross-asset ranking.

The evidence separates into two levels that tell a consistent story. \textbf{At the forecast level}, apparent gains from asymmetric model selection do not survive an honest out-of-sample test. In-sample and same-window comparisons make a $\gamma$-sign selection rule look attractive, but when the rule is frozen before a genuinely future evaluation window and the resulting comparisons are subjected to a Harvey-style multiple-testing correction, \emph{no asset} in the primary universe shows a statistically significant out-of-sample superiority of the selected model over the symmetric benchmark. The rule is best described as a risk-controlled model-selection diagnostic---it rarely does materially \emph{worse}---rather than as a source of forecasting edge. A significant asymmetry is neither necessary nor sufficient for a decision-relevant gain: cryptocurrency carries a significant $\gamma$ yet its competing models are indistinguishable out of sample, while the small primary cross-section ($N=6$) leaves any apparent classification success statistically underpowered. We therefore report equal-accuracy non-rejection as exactly that, and keep it separate from the significant superiority the data do not support.

\textbf{At the allocation level}, the same null holds in a more constructive form. The dominant and \emph{robust} benefit of volatility targeting---drawdown reduction---is driven by an asset's base volatility level across all tested assets, and is essentially independent of asymmetric structure. The measurement refinement that a modeller works hardest to get right is, at the decision level, largely orthogonal to the benefit the allocator actually captures: once a forecast captures the first-order level of volatility, additional model sophistication rarely improves the allocation outcome enough to matter. The positive content of the paper is therefore about \emph{where the value actually lives}---in the volatility level---rather than in the asymmetric structure the taxonomy refines.

Reading the asymmetry parameter itself as an economic object is a useful \emph{interpretive} device rather than a forecasting engine, and we treat it as such. The sign of $\gamma$ is economically legible---balance-sheet leverage for equities ($\gamma>0$), fear-driven flight-to-quality for gold ($\gamma<0$, conditional on stress regimes), coupon-dominated pricing for bonds ($\gamma\approx0$)---and within homogeneous equity markets it predicts whether volatility targeting behaves as trend-following or contrarian. But this mapping is a genuine domain restriction: it does not survive extension to heterogeneous asset classes, and gold's inverted asymmetry is regime-dependent rather than an unconditional property (the canonical rolling-window mean is statistically indistinguishable from zero). We therefore present the leverage-direction taxonomy as supporting structure that helps interpret \emph{where} the ceiling binds, not as the paper's headline claim.

The contribution is thus twofold: a reproducible measurement-to-allocation evaluation framework---one that carries a forecast all the way from a statistical loss function to an explicit allocation decision on the same assets and windows---and the honest, decision-relevant null it delivers. For researchers, the null is direct out-of-sample evidence that in-sample model refinement does not reliably survive to the allocation level, which cautions against equating better conditional-variance fit with better decisions. For practitioners, it argues for concentrating modelling effort on capturing the volatility level robustly rather than on asymmetric fine-tuning whose economic payoff is conditional at best. The result is made credible by a pre-specified evaluation protocol and a self-contained replication package rather than by an ex-post search over specifications.

The remainder of the paper is organized as follows. Section~\ref{sec:literature} situates the study in the forecasting-evaluation, risk-management, and volatility-targeting literatures. Section~\ref{sec:data_methodology} describes the data and the pre-specified out-of-sample methodology. Section~\ref{sec:empirical_results} presents the forecast-level and allocation-level evidence and the interpretive leverage-direction taxonomy. Section~\ref{sec:implications} discusses implications and limitations. Section~\ref{sec:conclusion} concludes.


% =====================================================================
% SECTION STUBS — ported incrementally in later ticks (see TICK TRACKER)
% Do NOT bulk-copy body.tex here; each port is a judgment step (reframe +
% sample-map reconciliation + wedge-first restructuring), done main-thread.
% =====================================================================

\section{Literature Review}\label{sec:literature}

This study draws on four literatures, which we order to foreground the question of when---and whether---added model complexity earns its keep once a forecast is carried through to a decision.

The first, and for our purposes the organising, literature is forecast evaluation. Robust comparison of volatility forecasts requires loss functions that preserve the true ranking under a noisy variance proxy, and the QLIKE loss has become the standard choice because its ranking is invariant to the proxy as long as the proxy is conditionally unbiased \citep{patton2011}. Formal inference on predictive accuracy follows \citet{diebold1995}, and the model confidence set \citep{hansen2011} recognises that a band of models is often statistically indistinguishable from the best rather than a single winner. A parallel methodological literature warns that comparisons drawn from a large specification search invite data mining and must be disciplined with multiple-testing corrections \citep{harvey2016}. What this literature establishes carefully at the forecast level it rarely carries through to the decision level: evaluations typically stop at accuracy comparisons, and allocation studies typically fix a single volatility model rather than asking whether the choice of model---the very complexity a modeller adds---is what moves the economic result. Our design is built to close exactly that gap, by observing the pass-through from measurement to allocation on the same assets, over the same periods, under one protocol.

The second literature concerns the specific measurement refinement we test: asymmetric volatility dynamics and the sign of the asymmetry. GARCH captures volatility clustering \citep{bollerslev1986}, while GJR-GARCH and EGARCH let negative and positive shocks affect variance differently \citep{glosten1993,nelson1991}. In equities this leverage effect is standard and economically tied to balance-sheet amplification \citep{black1976,christie1982,engle2018}. A smaller literature shows that non-equity assets can reverse the sign of the asymmetry, notably gold and some commodities, but stops short of turning that sign into a model-selection rule \citep{chevallier2017,batten2010,chang2021}. We treat the sign and magnitude of the asymmetry parameter as an economically interpretable object rather than as a forecasting engine---a device for reading \emph{where} a complexity ceiling binds---and so position this taxonomy as supporting structure rather than as the headline gap the paper fills.

The third literature concerns risk management, where the distributional assumption can matter as much as the variance equation. Basel backtesting places the tail model on an equal footing with the conditional-variance dynamics \citep{bcbs2006,bcbs2019,kupiec1995,christoffersen1998}, and Student-$t$ and skewed-$t$ innovations often improve tail coverage over Gaussian specifications \citep{bollerslev1987,hansen1994,kuester2006}. This strand is directly relevant to our VaR evidence, where the choice of innovation distribution dominates the choice of asymmetric variance dynamics.

The fourth literature studies volatility targeting, which we use as the allocation-level testbed. Inverse-volatility scaling improves drawdown control and sometimes improves risk-adjusted returns, though the evidence is criterion- and asset-dependent \citep{fleming2001,fleming2003,moreira2017,harvey2018,cederburg2020,bozovic2024,demiguel2024,hood2025,xu2024}. Our contribution is to connect these four strands through a single empirical object and a single decision: we ask not only whether asymmetric measurement forecasts better, but whether that forecasting edge survives to the volatility-targeting allocation---and we find that it largely does not, because the dominant allocation benefit tracks an asset's base volatility level rather than its asymmetric structure. This is the sense in which the paper documents a complexity ceiling.


\section{Data and Methodology}\label{sec:data_methodology}

Our design is built so that the pass-through from volatility \emph{measurement} to portfolio \emph{allocation} can be observed on the same assets under a single evaluation protocol, with the same one-step forecasts feeding both the forecast-level loss and the allocation weights. Forecast-evaluation windows are common across assets; allocation windows are each asset's own maximal out-of-sample span where history differs (Section~\ref{sec:allocation_results}), and we treat that heterogeneity as part of the null context rather than as a like-for-like ranking. We describe, in order, the data and a single canonical sample partition, the two competing volatility measurements, the rolling out-of-sample forecasting scheme and its timing, the forecast-level evaluation criteria, and the allocation-level testbed.

\subsection{Data}
\label{sec:data}

We use daily adjusted closing prices for seven exchange-traded assets spanning four asset classes: US equities---SPDR S\&P 500 ETF Trust (SPY), Invesco QQQ Trust (QQQ), and iShares MSCI Emerging Markets ETF (EEM); precious metals---SPDR Gold Shares (GLD) and iShares Silver Trust (SLV); government bonds---iShares 20+ Year Treasury Bond ETF (TLT); and cryptocurrency---Bitcoin (BTC-USD). Prices are sourced from Yahoo Finance for January 2017 through March 2026, and daily returns are computed as simple percentage changes. A validation exercise extends the cross-section to twenty-six assets. The choice of large, liquid ETFs and Bitcoin is defensible for a daily study because stale prices and delayed corporate-action adjustments---the usual hazards of adjusted-close data---bite hardest in microcaps rather than in these instruments \citep{bali2016}; we nonetheless validate the feed by spot-checking crisis dates, matching the VIX series to its official values, and confirming that return moments line up with overlapping benchmark studies \citep{moreira2017,cederburg2020}.

Table~\ref{tab:desc} reports descriptive statistics over the full sample. All return series reject normality (Jarque--Bera $p<0.001$), exhibit significant ARCH effects (Engle's LM test, $p<0.001$; \citealt{engle1982}), and are stationary (ADF $p<0.001$). Excess kurtosis ranges from 3.52 (GLD) to 14.61 (SPY), which motivates the fat-tailed innovations we consider for tail-risk measurement.

\paragraph{Sample partition (single canonical map).}
To keep every table and figure on a common footing, we fix one partition and refer all results to it. The full sample runs from January 2017 to March 2026 and is split into three non-overlapping windows: an \emph{in-sample training} window (2017-01 to 2022-12), a \emph{main out-of-sample} window (2023-01 to 2024-12), and an \emph{out-of-sample validation} window (2025-01 to 2026-03). Descriptive statistics (Table~\ref{tab:desc}) are computed over the full sample; every forecast-evaluation and allocation result is computed on the out-of-sample windows only, with parameters estimated exclusively on data preceding each forecast origin (Section~\ref{sec:rolling}). Where a table or figure reports a shorter span, it is a labelled sub-window of this partition; no reported result mixes in-sample and out-of-sample dates. The leverage-direction classification exercise (Section~\ref{sec:empirical_results}) is deliberately disjoint---models estimated on 2023 data classify a held-out 2024--2025 sample---so its training and evaluation periods never overlap. This window is shorter than some long-horizon GARCH studies, but each forecast uses a rolling two-year estimation window and robustness checks extend the effective history to 2006. The object of interest is not long-horizon return predictability but whether the sign and significance of the asymmetry parameter $\gamma$ carries useful information into a decision, and that object is stable across the window sizes we test and across stressed subperiods such as 2020--2022 and the 2025--2026 Iran/Hormuz episode.

\subsection{The two measurements}

The added complexity we test is asymmetry in the conditional variance. The benchmark measurement is the symmetric GARCH(1,1) of \citet{bollerslev1986}, $\sigma^2_t=\omega+\alpha\varepsilon^2_{t-1}+\beta\sigma^2_{t-1}$. The refined measurement is the GJR-GARCH(1,1) of \citet{glosten1993},
\begin{equation}
\sigma^2_t = \omega + (\alpha + \gamma\,\mathbf{1}_{\varepsilon_{t-1}<0})\,\varepsilon^2_{t-1} + \beta\sigma^2_{t-1},
\end{equation}
in which $\gamma$ measures the asymmetric response to signed shocks: $\gamma>0$ is the standard leverage effect (negative returns raise variance), while $\gamma<0$ is an inverted effect. The step from GARCH to GJR is the smallest and most widely used move up the complexity ladder, which makes it the natural place to ask whether refined measurement pays off downstream.

\paragraph{Mean specification.}
We adopt the zero-mean specification $r_t=\sigma_t\varepsilon_t$ throughout, because the conditional mean of daily returns is negligible relative to conditional variance: for a typical daily mean of $0.04\%$ and daily standard deviation of $1\%$, the mean-to-volatility ratio is about $0.04$, making its contribution to variance dynamics immaterial \citep{bollerslev1986,hansen2005}. \citet{francq2004} show that quasi-maximum likelihood estimation of GARCH parameters remains consistent regardless of the mean specification when the true conditional mean is small relative to conditional variance. As a robustness check we re-estimate all models with an AR(1) mean, $r_t=\mu+\phi r_{t-1}+\sigma_t\varepsilon_t$; the QLIKE rankings, $\gamma$-sign classifications, and Diebold--Mariano conclusions are all preserved (Section~\ref{sec:empirical_results}, robustness). All models are estimated by Normal quasi-maximum likelihood using the \texttt{arch} package \citep{sheppard2023}; for Value-at-Risk we additionally consider Student-$t$ innovations with fixed degrees of freedom.

\subsection{Rolling out-of-sample forecasts and timing}
\label{sec:rolling}

Forecasts are generated with a rolling window that is re-estimated at every forecast origin. The primary window is $w=504$ trading days (about two calendar years), which satisfies the minimum-sample recommendation for GARCH estimation ($\geq 500$ observations; \citealt{hwang2006}) while limiting contamination from distant regime changes. Robustness checks with $w\in\{252,1000,2000,3000,5000\}$ (online supplement) confirm that QLIKE rankings are qualitatively robust to the window choice, with $w=504$ best during high-volatility periods (2020--2021) and larger windows ($w\geq 2000$) marginally better in calm periods (2023--2024); the $\gamma$-sign classification is invariant to window size across all tested values.

For each out-of-sample date $t$ we estimate each model on the window $[t-w,\,t-1]$ and form a one-step-ahead conditional-variance forecast $\hat{\sigma}^2_t$: the forecast origin is $t-1$ and the horizon is a single trading day. Estimation therefore uses only information available strictly before the forecast target, so the scheme carries no look-ahead. The realized-variance proxy is the squared daily return $r^2_t$, and forecasts are aligned to the target return date before any loss is computed.

\subsection{Forecast-level evaluation}

\subsubsection{Statistical loss function}

The primary evaluation metric is the QLIKE loss \citep{patton2011}, expressed as the Gaussian quasi-log-likelihood contribution:\footnote{This form equals $-\ln f(r_t\mid\hat{\sigma}^2_t)$ under a Gaussian quasi-likelihood, up to an additive constant. Values are negative for daily returns because $\ln\hat{\sigma}^2_t\approx\ln(10^{-4})\approx-9.2$. \textbf{Lower (more negative) values indicate better forecasts.} An alternative convention writes the centered loss as $L^*(\sigma^2,h)=\sigma^2/h-\ln(\sigma^2/h)-1$, which centers near $1.0$. Both preserve forecast rankings; we adopt the quasi-log-likelihood form throughout for consistency with maximum-likelihood estimation.}
\begin{equation}
\text{QLIKE} = \frac{1}{T}\sum_{t=1}^{T}\left(\frac{r^2_t}{\hat{\sigma}^2_t} + \ln\hat{\sigma}^2_t\right).
\label{eq:qlike}
\end{equation}
QLIKE is robust to noise in the realized-variance proxy and ranks forecasts consistently regardless of the proxy used, provided the proxy has equal bias across the models compared.

\subsubsection{Test of equal predictive accuracy}

We test the null of equal predictive accuracy between two forecasts with the Diebold--Mariano test \citep{diebold1995} using Newey--West HAC standard errors (five lags):
\begin{equation}
DM = \frac{\bar{d}}{SE_{NW}(\bar{d})} \sim N(0,1),
\end{equation}
where $d_t = L(r^2_t,\hat{\sigma}^2_{1,t}) - L(r^2_t,\hat{\sigma}^2_{2,t})$ is the QLIKE loss differential. Because the forecast-level cross-section is small, comparisons that draw on many specifications are additionally disciplined against data mining with a multiple-testing correction \citep{harvey2016}, and we read a band of statistically indistinguishable models rather than a single winner where one exists \citep{hansen2011}.

\subsubsection{Value-at-Risk backtesting}

Value-at-Risk at confidence level $\alpha$ is computed as
\begin{align}
\text{Normal:}\quad & \text{VaR}_\alpha = \Phi^{-1}(\alpha)\,\hat{\sigma}_t, \\
\text{Student-}t\text{:}\quad & \text{VaR}_\alpha = t^{-1}_\nu(\alpha)\,\sqrt{(\nu-2)/\nu}\,\hat{\sigma}_t,
\end{align}
where $\nu$ is the degrees of freedom (fixed at five in the baseline). This choice is validated out of sample: Basel III Green-Zone compliance holds for all $\nu\leq 6$, and jointly estimating $\nu$ produces \emph{worse} performance by over-adapting to quiet markets, so a fixed $\nu=5$ is a conservative choice robust across $\nu\in[4,7]$ (Section~\ref{sec:empirical_results}). We apply \citeauthor{kupiec1995}'s (\citeyear{kupiec1995}) unconditional-coverage test and \citeauthor{christoffersen1998}'s (\citeyear{christoffersen1998}) independence test, classifying annual violation counts by the Basel III traffic-light system (Green 0--4, Yellow 5--9, Red $\geq 10$ per $\approx 250$ trading days).

\subsubsection{Leverage-direction classification}

As the interpretive device that locates \emph{where} the ceiling binds, we characterise the temporal stability of the asymmetry by estimating GJR-GARCH on semi-overlapping quarterly windows (504-day windows advanced by 63 trading days) and collecting the $\gamma$ estimates. We test $H_0\!:\,E[\gamma]\geq 0$ against $H_1\!:\,E[\gamma]<0$ (inverted leverage) with a one-sided $t$-test on the quarterly $\gamma$ series, and separately evaluate whether the sign of $\gamma$ estimated on 2023 data correctly classifies out-of-sample behaviour on the disjoint 2024--2025 window.

\subsection{Allocation-level evaluation: volatility targeting}
\label{sec:vt_methodology}

Volatility targeting is our decision-level testbed: it is where we observe whether a forecasting edge survives to the allocation. Following \citet{moreira2017}, the volatility-managed weight is
\begin{equation}
w_t = \frac{\sigma_{\text{target}}}{\hat{\sigma}_t},
\end{equation}
with $\sigma_{\text{target}}=10\%$ annualised for the GARCH-based analysis and $\sigma_{\text{target}}=12\%$ for the VIX-based analysis (Section~\ref{sec:empirical_results}), the latter reflecting the VIX's systematic upward bias from the embedded variance risk premium. Weights are smoothed with a five-day moving average and clipped to $[0,1.5]$. A post-hoc transaction-cost analysis confirms that the estimated annual turnover of $756\%$ generates cost drag of only 8--15 basis points at typical SPY bid--ask spreads. Because the same one-step-ahead forecasts feed both the QLIKE evaluation and the targeting weights, any wedge between forecast-level accuracy and allocation-level benefit is attributable to the decision rule, not to a change in the underlying forecast.


\section{Empirical Results}\label{sec:empirical_results}

\subsection{Data Characteristics}
\label{sec:data_characteristics}

Table~\ref{tab:desc} summarizes the distributional properties of daily returns for the seven primary assets over the full descriptive-statistics sample (2017-01 to 2025; the partition of Section~\ref{sec:data} governs all forecast- and allocation-level results below).% source: experiments/k903/tables (canonical descriptive-stats replication)
All series show substantial excess kurtosis, from 3.5 (GLD) to 14.6 (SPY), and reject normality; annualized volatility spans an order of magnitude, from 14.6\% (GLD) to 57.3\% (BTC-USD) over this sample. Section~\ref{sec:allocation_results} instead uses each asset's volatility-targeting evaluation-window level---which for BTC-USD's post-2019 VT sample is 51.7\%, a different window than the descriptive-statistics sample---as the basis for testing whether base volatility level, rather than asymmetric structure, drives the allocation benefit.

\subsection{Forecast-Level Results: Conditional Gains from Asymmetric Measurement}
\label{sec:forecast_results}

Table~\ref{tab:gamma} reports rolling-window GJR-GARCH $\gamma$ estimates (504-day windows, quarterly step, 2010--2026; Newey--West HAC $t$ with 8 lags to correct for the 87.5\% window overlap).% source: experiments/k903/tables/k903_table2.csv
The sign is consistent within asset class: SPY ($\gamma=+0.132$, $t=+11.08$), QQQ ($+0.116$, $t=+10.76$), and EEM ($+0.087$, $t=+11.88$) show standard, statistically unambiguous leverage. GLD's canonical estimate is $+0.002$ ($t=+0.15$, 67\% of windows negative)---indistinguishable from zero---and TLT and SLV are likewise centered near zero ($-0.005$ and $-0.009$).\footnote{An earlier draft reported GLD mean $\gamma=-0.067$ ($t=-5.79$, 93\% negative windows) for the same specification; the canonical re-estimation in the replication package reverses the sign to $+0.002$ and renders it insignificant. We adopt the corrected value throughout; Section~\ref{sec:interpretive_taxonomy} shows the economically interpretable result is a regime decomposition, not the unconditional mean.} BTC-USD shows a small but significant positive asymmetry ($+0.072$, $t=+2.88$) with high cross-window instability (std $=0.105$, 25\% negative windows).

Table~\ref{tab:qlike} takes this taxonomy to an out-of-sample forecasting test: GJR-GARCH versus symmetric GARCH(1,1) QLIKE, evaluated on the main OOS window (2023--2024) and the validation window (2025).% source: experiments/k903/tables/k903_table3.csv
The pattern tracks $\gamma$: for SPY, GJR wins significantly in both periods (DM $p=0.003$ and $p=0.048$). For GLD, whose mean $\gamma$ is statistically zero, the comparison \emph{reverses}---symmetric GARCH significantly outperforms GJR in 2023--2024 ($p=0.001$) and directionally in 2025 ($p=0.070$): imposing an asymmetry term on an asset without stable asymmetry actively hurts forecasts. TLT is a near-exact tie in both periods, consistent with its near-zero $\gamma$. QQQ is directionally consistent with GJR's advantage but does not reach significance in either period ($p=0.181$, $p=0.086$) and should be read as illustrative. BTC-USD is the sharpest counter-example to a naive ``significant $\gamma$ implies forecasting value'' reading: despite a statistically significant asymmetry, the two models are indistinguishable out of sample ($\Delta=-0.06\%$, $p=0.848$)---a significant $\gamma$ is necessary for GJR to plausibly help, but not sufficient, when the estimated asymmetry is small in magnitude and unstable across windows.

This motivates a simple selection rule: estimate GJR-GARCH on the current window; if $\gamma$ is positive and significant at $t>1.65$, use GJR-GARCH, otherwise use symmetric GARCH. It is essential to be precise about what this rule does and does not achieve. Applied to the quarterly-mean statistic in Table~\ref{tab:gamma}, the rule avoids the significant \emph{losses} that come from imposing an asymmetry term where none is stable---most visibly on GLD, where symmetric GARCH significantly beats GJR (Table~\ref{tab:qlike})---so the selected model is rarely significantly \emph{worse} than the benchmark. But once the eleven Diebold--Mariano comparisons in Table~\ref{tab:qlike} are disciplined with a Harvey-style multiple-testing correction, and once the selection is frozen before a genuinely future evaluation window rather than read off the same window used to estimate $\gamma$, no asset shows a \emph{statistically significant} out-of-sample superiority of the selected model over the symmetric benchmark. The rule's value is therefore defensive---a risk-controlled model-selection diagnostic, not a source of forecasting edge. We are explicit that this reverses the reading of an earlier same-window draft, which described the prescribed model as ``never significantly beaten'' and reported near-perfect $\gamma$-sign classification: those statements conflated three distinct outcomes---significant superiority, equal-accuracy non-rejection, and a small mean-loss edge that does not survive multiple-testing control---and only the two weaker outcomes are supported. With only $N=6$ primary forecasting assets, any classification exercise is in any case statistically underpowered, so we treat it as illustrative rather than confirmatory. Gamma sign classifications are unchanged under window sizes $w\in\{252,504,756\}$ and under an AR(1) mean specification, and the QLIKE ranking is preserved under the Parkinson range-based proxy, consistent with \citet{patton2011}.

\subsection{Allocation-Level Results: The Measurement-to-Allocation Wedge}
\label{sec:allocation_results}

We now ask whether the forecast-level gains of Section~\ref{sec:forecast_results} survive to the volatility-targeting allocation of Section~\ref{sec:vt_methodology}, using for each asset the GARCH specification prescribed by the selection rule above. Table~\ref{tab:vt} reports buy-and-hold versus VT performance for the five primary assets with sufficient history.% source: experiments/k903 VT cross-asset replication
Maximum drawdown improves for \emph{every} asset tested, and the size of the improvement is strongly correlated with the asset's base volatility level rather than with the sign or significance of its $\gamma$: Pearson $\rho=0.944$ ($p=0.016$) across the five assets in Table~\ref{tab:vt}, and $\rho=0.83$ ($p=0.0002$, Spearman $\rho=0.88$) across an extended 14-asset sample reported in the Online Supplement, which we treat as the primary basis for inference given its larger $N$. BTC-USD, the highest-base-volatility asset in the VT sample (51.7\% annualized), sees MaxDD improve by 55.3 percentage points ($-76.6\%\to-21.3\%$); TLT, among the lowest-volatility assets (15.0\%), improves by 13.1pp. Sharpe-ratio improvement is more heterogeneous and only weakly related to base volatility ($\rho=0.80$, $p=0.10$)---consistent with VT operating chiefly as a volatility-scaling mechanism rather than as a source of directional timing skill.

This is the clearest expression of the paper's null: the asset for which the forecast-level asymmetry is strongest and most significant (SPY, $\gamma=+0.132$) and the asset for which it is statistically indistinguishable from zero (GLD, $\gamma=+0.002$) both receive a large, level-driven drawdown benefit from volatility targeting. Whatever the asymmetric structure of an asset, it is the volatility \emph{level}, not that structure, that governs the size of the allocation benefit. We test this directly for gold, whose \emph{inverted} leverage during stress periods could plausibly undermine VT---if fear-driven rallies raise measured volatility, VT would cut gold exposure exactly when gold is performing its hedging function. Comparing standard VT against an ``anti-VT'' rule that instead increases gold exposure in high-volatility periods, standard VT achieves Sharpe 1.71 versus anti-VT's 1.51 and buy-and-hold's 1.56 over 2022--2026, and 0.62 versus 0.56 over the full 2010--2026 history: even when volatility is being driven by positive returns, elevated volatility still signals elevated risk (including reversal risk), so VT's risk-reduction channel dominates the opportunity cost of reduced exposure. Leverage direction affects \emph{how} VT re-weights around a trend (Section~\ref{sec:interpretive_taxonomy}), not whether the drawdown-reduction benefit materializes.

A parsimonious, model-free alternative reinforces the same point. Following \citet{moreira2017}, replacing the GARCH-based volatility estimate with the VIX, $w_t = \sigma_{\text{target}}/\text{VIX}_t$ with $\sigma_{\text{target}}=12\%$ (the ``12/VIX'' rule, equivalent to the VIX-managed portfolio of \citealt{bozovic2024} reframed within the VT paradigm), achieves Sharpe 0.856 versus GARCH-based VT's 0.826 and MaxDD $-16.5\%$ versus $-18.4\%$ across seven OOS periods (2009--2025). The Sharpe difference is not significant ($t=0.33$), but the MaxDD improvement is (bootstrap $p=0.0004$, 95\% CI $[+9.2\text{pp},+71.7\text{pp}]$). That a zero-parameter, implied-volatility rule matches or exceeds a carefully specified, asymmetry-aware GARCH model at the allocation level is itself evidence for the ceiling: the marginal modelling effort invested in getting the variance equation right buys little once the allocation only needs the volatility \emph{level}, not its asymmetric dynamics.\footnote{VIX-based backtests are vulnerable to a same-day timing bias of roughly $+1.0$ Sharpe unit when $\text{VIX}_t$ is applied to $r_t$ rather than $r_{t+1}$; all 12/VIX results reported here use properly lagged weights (DM $p=0.62$ against the same-day benchmark).}

A comparison against EWMA ($\lambda=0.97$) volatility estimates shows the same near-parity at the level of Sharpe and MaxDD over the primary OOS period (EWMA vs.\ GJR-GARCH: 0.828 vs.\ 0.782 Sharpe, DM $p=0.73$; $-12.3\%$ vs.\ $-12.5\%$ MaxDD), but a crisis-period divergence: during COVID-19, GJR-GARCH achieves Sharpe 1.130 and MaxDD $-13.9\%$ versus EWMA's 0.745 and $-18.8\%$. Holding the mean weight fixed and varying only weight-path smoothness, the correlation between smoothness and MaxDD is $\rho=-0.007$ ($p=0.87$)---smoothness \emph{per se} has no effect---so the crisis advantage is attributable specifically to the asymmetric $\gamma$ channel triggering faster deleveraging than EWMA's exponential decay, not to GJR weights simply reacting faster in general. The asymmetry term therefore does earn its keep, but as a crisis-period tail-risk feature rather than as a source of everyday Sharpe improvement---a more precise statement of when the added complexity pays off.

Finally, we test directly whether VT's allocation benefit reflects directional market timing rather than variance management. \citet{henriksson1981}, Treynor--Mazuy, and Merton-ratio tests over 2014--2026 ($N\approx3{,}100$) unanimously fail to reject the null of no directional timing ability ($p>0.70$); conditional on high-VIX episodes (VIX $>25$), the timing coefficient is significantly \emph{negative} ($\hat{\gamma}_{HM}=-0.068$, $t=-4.63$)---VT misses post-crisis recoveries, the opposite of successful timing. The allocation-level benefit of volatility targeting is a variance-management effect, not a forecasting or timing edge, consistent with it surviving even where the underlying volatility forecast carries no detectable asymmetric skill.

\subsection{Interpretive Taxonomy: Where the Ceiling Binds}
\label{sec:interpretive_taxonomy}

Although the sign of $\gamma$ does not predict the \emph{size} of the allocation-level benefit, it remains economically legible and helps explain \emph{how} volatility targeting re-weights around a trend for each asset class. For equities, $\gamma>0$ reflects balance-sheet leverage: falling prices raise debt-to-equity ratios and firm risk \citep{black1976,christie1982}, so VT cuts exposure after declines and restores it after rallies---a mechanical stop-loss. For gold, the canonical unconditional $\gamma$ is statistically indistinguishable from zero, but this masks regime dependence: partitioning the full 2005--2026 sample into bull and bear regimes yields bull $\gamma=-0.043$ versus bear $\gamma=+0.048$ ($t=-3.79$, $p<0.001$), consistent with gold's safe-haven role \citep{baur2010safe,baur2010hedge}---fear-driven flight-to-quality buying raises both price and measured uncertainty during stress, the mirror image of the equity leverage mechanism. SPY shows no such regime dependence (bull $\gamma=+0.24$, bear $\gamma=+0.24$, $p=0.95$), consistent with a mechanism that operates regardless of market direction. Treasury bonds (TLT) show no significant asymmetry in either direction, consistent with coupon-dominated pricing that does not systematically load onto directional sentiment.

Within the homogeneous equity domain, the sign of $\gamma$ also predicts whether VT re-weighting behaves as trend-following or contrarian: projecting VT weight changes on trailing five-day returns, the rank correlation between $\gamma$ and this trend-following coefficient is $\rho=0.886$ ($p=0.019$, $N=6$ equity-type assets), and the mapping estimated on 2010--2017 data predicts the 2018--2026 out-of-sample trend-following coefficient ($\rho=0.821$, $p<0.05$), ruling out a purely mechanical, same-sample artifact. This regularity does \emph{not} extend to a broader cross-section spanning commodities, bonds, and digital assets ($\rho=-0.448$, $p=0.14$, $N=12$): we therefore present it as a domain-restricted empirical regularity, not a universal theorem, and its restriction is itself an instance of the ceiling---the interpretive value of $\gamma$ travels only as far as the asset-class homogeneity that produced it.

\subsection{VaR Compliance: An Orthogonal Robustness Check}
\label{sec:var_compliance}

As a check that the forecast-level gains documented above are not an artifact of the QLIKE loss function, we also examine VaR compliance, which depends heavily on the assumed innovation distribution.% source: experiments/k903 VaR replication (frozen 2025-Q4 vintage)
Table~\ref{tab:var} decomposes SPY's 1\% VaR violation count (2020--2025, 1508 days) as successive refinements are added: Normal innovations produce 33 violations (2.2\%, against a nominal target of 15, or 1.0\%); moving to Student-$t$ ($df=5$) reduces this to 18 (1.2\%); adding adaptive volatility thresholding and jump augmentation together reduces violations to 14 (0.9\%), closely approaching, though not exactly matching, the nominal target. Table~\ref{tab:var_panel} extends the comparison across the seven-asset panel and five distributional methods under a Trinity criterion (Kupiec, Christoffersen, and dynamic-quantile tests jointly; \citealt{kupiec1995,christoffersen1998}): Skewed-$t$ and filtered historical simulation achieve the highest joint pass rates (90.5\% and 76.2\% of asset--$\alpha$ cells), against 57.1\% for the Normal benchmark. The full GARCH-versus-GJR orthogonality decomposition under fixed distributional assumptions is reported in the Online Supplement; together with the forecast- and allocation-level results above, it points to the same conclusion: added model complexity pays off, if at all, only within the specific dimension it targets, and does not transfer automatically to other decision-relevant criteria.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{fig_rolling_gamma.pdf}
\caption{Rolling 252-day GJR-GARCH $\gamma$ estimates for SPY, GLD, TLT, and EEM (2010--2026). SPY maintains consistently positive $\gamma$ (standard leverage), GLD fluctuates between positive and negative (regime-dependent inverted leverage), and TLT remains near zero. Shaded regions indicate $\gamma < 0$.}
\label{fig:rolling_gamma}
\end{figure}


\section{Implications and Limitations}\label{sec:implications}

\subsection{The Complexity Ceiling: Synthesis}
\label{sec:ceiling_synthesis}

Three independent pieces of evidence in Section~\ref{sec:empirical_results} point to the same conclusion. First, the forecast-level gain from asymmetric modelling does not survive an honest out-of-sample test: any apparent same-window advantage of $\gamma$-based model selection disappears once the selection is frozen ahead of the evaluation window and the comparisons are corrected for multiple testing, and a statistically significant asymmetry is not even sufficient for a forecasting edge (BTC-USD has significant $\gamma$ yet models that tie out of sample). Second, the allocation-level benefit from volatility targeting is close to universal and is governed by an asset's base volatility level, essentially orthogonal to whether that asset has significant, zero, or inverted asymmetry. Third, a parsimonious, zero-parameter alternative (12/VIX) matches the carefully specified asymmetric GARCH model at the allocation level. Each piece independently narrows the space in which added modelling complexity could earn its keep, and together they describe a ceiling: complexity that improves a statistical criterion (QLIKE) need not, and here does not, improve the decision built on top of it.

A sharper illustration of the same wedge comes from comparing loss functions directly. Using absolute returns as the realized-volatility target, an HAR specification outperforms GJR-GARCH(1,1) on QLIKE in all seven markets tested (SPY, QQQ, EFA, EWZ, GLD, TLT, 0050.TW; Diebold--Mariano statistics from $-11.11$ to $-21.74$, all $p<0.001$); yet the same HAR specification produces the \emph{lowest} volatility-targeting Sharpe ratio among the alternatives compared (1.12, versus 1.75 for 12/VIX), with $1.9\times$ higher turnover and a weight-path correlation of only 0.51 with 12/VIX. HAR's forecast-accuracy advantage is backward-looking: it aggregates historical return magnitudes efficiently across daily, weekly, and monthly horizons. The allocation gain instead rewards forward-looking risk pricing, which a purely backward-looking aggregator does not supply. A model can win decisively on the criterion a researcher optimises for and lose on the criterion an allocator actually needs; full detail is reported in the Online Supplement.

\subsection{Practitioner and Researcher Takeaways}
\label{sec:takeaways}

For practitioners, the actionable message is to prioritise getting the volatility \emph{level} right --- with a GARCH model, an EWMA average, or a model-free VIX-based rule, whichever is operationally convenient --- over fine-tuning the asymmetric response. Asymmetric structure is not worthless: it is a genuine crisis-period risk-management feature (Section~\ref{sec:allocation_results} shows GJR-based targeting outperforming EWMA specifically during COVID-19, driven by faster deleveraging through the $\gamma$ channel), and its economic sign is informative about the mechanism driving an asset's price (balance-sheet leverage, safe-haven flows, or coupon-dominated neutrality). But it should be budgeted as a targeted, verifiable refinement --- justified asset by asset via a significance screen on $\gamma$ --- rather than treated as a default source of everyday allocation value, consistent with \citet{hood2025} and \citet{nelson2025}'s characterisations of volatility-managed strategies as risk-control rather than forecasting engines.

For researchers, the message is methodological. A forecast-accuracy comparison, however carefully executed, is not evidence of decision-level value on its own; the pass-through from measurement to allocation has to be checked directly, on the same assets and periods, because it can and does fail even when the measurement result is genuine and correctly signed. In-sample or forecast-accuracy-only refinement that has not been carried through to an explicit decision rule should be reported and interpreted as exactly that --- a measurement result --- not extrapolated into an implied economic claim.

\subsection{Limitations}
\label{sec:limitations}

The forecast-level classification exercise of Section~\ref{sec:forecast_results} is based on a six-asset cross-section, so its formal statistical power is limited (even random classification succeeds with probability $2^{-6}\approx1.6\%$); we report it as supportive rather than decisive evidence and do not claim the specific $t$-statistic threshold generalises without independent validation on a larger asset universe. The gold safe-haven regime decomposition of Section~\ref{sec:interpretive_taxonomy} rests on a single asset, and the equity-only trend-following/contrarian regularity is explicitly domain-restricted: it holds within the six equity-type assets we test and fails outside that domain ($\rho=-0.448$ across a broader twelve-asset cross-section including bonds, commodities, and crypto). Neither should be extrapolated to asset classes outside the ones tested here. Our price data are sourced from Yahoo Finance rather than an institutional-grade vendor (CRSP, Bloomberg); validation checks show high concordance for large-cap ETFs, but replication with institutional data would strengthen confidence in the exact reported magnitudes. Finally, this study draws on a broader experimentation program; our primary claims are disciplined by a pre-specified Harvey-style significance threshold and by testing across two non-overlapping out-of-sample periods, but the window-size and proxy-choice robustness checks reported alongside the main results were not all pre-registered and should be read as corroborating rather than confirmatory \citep{harvey2016}. The asset universe itself is restricted to equities, precious metals, government bonds, and cryptocurrency; extending the taxonomy to currencies and a broader commodity set is a natural next step that we leave to future work.


\section{Conclusion}\label{sec:conclusion}

We asked a narrow question: when does better volatility measurement actually improve allocation? Across a pre-specified, out-of-sample design spanning seven primary assets and four asset classes, the honest answer is that it rarely does. Moving from a symmetric GARCH(1,1) to an asymmetric GJR-GARCH(1,1), and selecting between them on the sign and significance of the asymmetry parameter, delivers no statistically significant out-of-sample forecast superiority once the selection is frozen ahead of the evaluation window and the comparisons are corrected for multiple testing; the selection rule is best read as a defensive, risk-controlled diagnostic rather than as a forecasting edge. At the allocation level, the dominant benefit of volatility targeting --- drawdown reduction --- is close to universal across our sample and is governed by an asset's base volatility level rather than by its asymmetric structure, to the point that a zero-parameter, implied-volatility rule matches a carefully specified asymmetric GARCH model. The sign of the asymmetry parameter remains a genuinely useful, economically legible device for reading which mechanism --- balance-sheet leverage, safe-haven flight-to-quality, or coupon-dominated neutrality --- is driving an asset's price, but it is an interpretive object, not a forecasting or allocation engine, and its cross-asset regularities are domain-restricted rather than universal. The paper's contribution is accordingly a method and a null: a reproducible protocol that carries volatility measurement all the way to an allocation decision on the same assets and windows, and the honest finding that, once the first-order volatility level is captured, added model sophistication seldom earns its keep. Researchers and practitioners are both better served by testing for decision-level value directly than by assuming that a better in-sample fit is itself the decision that matters.

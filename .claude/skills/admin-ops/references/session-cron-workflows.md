# Retired Compatibility Pointer

此路徑只為既有 cross-reference 保留，不再定義可執行 workflow。

正式週期工作請讀：

- `config/runtime_schedules.json`
- `docs/architecture.md` 的 Operations Core
- `references/scheduling.md`

互動 turn 只完成當前工作並回覆；它不建立或維護 business clock。要新增週期需求，走
canonical schedule proposal／owner transaction，並以 terminal fire receipt 與下游
readback 驗證。
